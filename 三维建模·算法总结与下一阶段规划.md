# 三维建模：算法总结与下一阶段规划

> 写于 2026-09-10，c019 门系统修复 + 图纸回归事故修复之后。
> 对象：D:\gym3d 校园建筑数字孪生平台（DXF/DWG → 三维构件 → GLB + 室内导航）。
> 现状规模：**48 栋 / 292 层 / 48 个 GLB**，层数分布 {6层:34, 7层:7, 3层:2, 5层:2, 4层:1, 8层:1, 11层:1}。

---

## 一、现在的算法在做什么

### 1.1 管线全貌

```
DWG ──ODA File Converter──► DXF(audit=1)
                              │
                    ┌─────────┴──────────┐
                    │  recognizer/       │  图元 → 构件分类
                    │   classify.py      │  LWPOLYLINE 基线
                    │   classify_line.py │  LINE+INSERT 约定（c006 逸夫楼）
                    │   → 墙线 / 门 / 楼梯 / 柱（**只是线，还没有厚度**）
                    │                    │
                    │   floor.py         │  逐层切割 + 语义
                    │   extract_floor()  │  轮廓 / 外内墙 / 门 / 窗 / 柱 / 楼梯井 / 房间
                    │   _door_boxes()    │  门洞盒（本次新增）
                    │                    │
                    │   geometry.py      │  pair_wall_faces() 双线配对 → 真实厚墙
                    │   component_library│  构件库（门符号、踏步线剔除判据）
                    │   curve_walls.py   │  弧墙/斜墙配对
                    │   profile.py       │  每栋一配置 BuildingProfile
                    └─────────┬──────────┘
                              │
                    _wall_thin_batch.py / _wall_thin_force.py
                              │  内墙重建（recognize 的内墙是"粗环 blob"，不可用）
                              │  + _punch_doors() 门洞挖穿（本次新增）
                              ▼
                    run_building.validate()  ──►  build_standard_glb.py  ──►  GLB
                              │
                    qa_structural.py  I1–I10 不变量门禁（只读）
                    _dxf_cad_render.py 忠实源图纸 / _dxf_compare_render.py 识别对比
```

### 1.2 核心判据的原理（这是最值得固化的部分）

**① 墙 = 双皮配对，不是单线加粗**

图纸里一道墙画成两条平行线（内皮/外皮），中间是空的。`G.pair_wall_faces()` 把间距落在
`[wall_min, wall_max]` 的平行线段配成对，取两皮实测间距为墙厚，生成矩形。
—— 这一步决定了"墙是不是真的墙"，是整条管线质量的天花板。

配套的三道剔除（都是踩坑换来的）：
- **门符号剔除**：门扇线 + 门垛闭合环和墙同图层，会被配成 0.9~1.5m 的假墙正盖在门洞上
  （c019 每层约 285 块）→ `is_door_symbol_pts()` 按折线形状判。
- **踏步假墙剔除**：楼梯踏步线间距 0.3m 正好落进配对区间，会配成假墙塞满楼梯井
  → 按"同 y 聚 ≥4 条 + 对称三线"在**多段线粒度**先剔再拆段。
- **内皮线过滤**：曾经用"最小距离"过滤，误杀垂直隔墙致每层内墙少一半
  → 改"中点距离"后 c0xx 墙数 45→68 / 49→67。

**② 门 = gap 不是 hole（本次 c019 修复的核心洞察）**

门洞盒的深度取 `max(外墙厚, 内墙厚) + 0.05`，**比墙厚更深**。这样 `difference` 之后
墙是被**切断成两段**，而不是留下一个封闭的洞。若深度小于墙厚，shapely 会留一个
"洞中洞"，布尔运算在贴边情形下会不稳定，且渲染层无法据此补门头过梁。

**③ 三条判据路径必须共享同一套几何**

修 c019 时暴露的根本问题：
- `door_by_points`（理化楼专有）与 `detect_doors`（其余楼）是**两条互不相通的判据**；
- `floor.py` 里整个门洞盒代码块被 `getattr(p, "door_by_points", False)` 关着
  —— 于是 LWPOLYLINE 楼（c019 等）**从来没挖过门洞**，墙是连续的，门板被埋进实心墙里
  = 用户报的「门有的夹在墙里看不到」；
- 而交付链的内墙是由 `_wall_thin_*` 从 DXF **重新配对**生成的，它不认识门
  —— 就算 recognize 里挖了洞，也会被这一步冲掉。

修法：把门洞盒抽成 `_door_boxes()` 供两条路径共用（4000 例随机等价性验证，不符 0）；
在 `_wall_thin_batch` 里加 `_punch_doors()` 补上交付链那一刀。

**c019 修复效果**

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 门数 | 785 | **269** |
| 门洞挖穿率（±0.4m 沿墙走向都出墙） | 51/785 = 6% | **243/269 = 90%** |
| 1.5m"门"（实为窗户/墙垛） | 340 扇 | **0** |
| 实体墙面积损失 | — | 3.15~5.02%（= 门洞 6.6~9.8㎡ + 碎屑 0.39~0.70㎡）|

---

## 二、诊断：五个结构性问题

这一节是规划的依据。每条都有实据，不是泛泛而谈。

### 问题 1：管线断裂——"墙的真相"分散在三个互不相识的阶段

`recognize()` 产出的内墙是"粗环 blob"（`_wall_thin_batch.is_blob_floor` 判 cov>20%），
真实内墙要由 `_wall_thin_force.py` 从 DXF 重新配对。于是：
- 门洞挖穿必须**在两处都实现**（recognize + rebuild），漏一处就被冲掉；
- `_wall_thin_*` 重built 的内墙**没有 `id` 字段**（schema 违约），
  导致 `build_standard_glb.build_walls` 第 149 行 `KeyError: 'id'`。

**这是本次修 c019 时连续踩的两个坑，根因都是"阶段之间没有契约"。**

### 问题 2：分类器分裂 + 特例开关堆叠

| 维度 | 分叉 | 后果 |
|---|---|---|
| 图元类型 | `classify.py`（LWPOLYLINE） vs `classify_line.py`（LINE+INSERT） | 每栋楼一行 `getattr(p,"classifier")` 特例 |
| 门判据 | `detect_doors` vs `door_by_points` | 后者专有代码块关住了前者的功能（c019 bug） |
| 弧墙 | `pair_curved` 开关，只吃斜段 | c072/c073→100%，c041 仅 73.4% |
| 轮廓 | `outline_unify_floors` 开关 | 关掉会让 c009 墙数从 ERROR 37 反弹到 121 |
| 窗 | `INCLUDE_SYNTHETIC_WINDOWS` | 模块默认 True，`run_building --glb` 默认 False，口径不一致 |

**每一个开关都是"某栋楼独有"，加在一起就是 48 栋楼 48 种行为。**
根目录 **100+ 个 `_*.py` 一次性脚本**是同一症状的另一面——修复无法沉淀成能力，只能沉淀成脚本。

### 问题 3：没有"对象"概念——构件被当成多边形集合

墙是 `{poly, holes}` 的列表，不是"一条有轴线的墙"。于是：
- 两道墙相交处**各自独立成矩形、互相重叠**，没有 T 型/L 型接合；
- 门、窗、洞**各自算盒**，没有共享模型 → 才会出现"窗被当门"（c019 的 1.5m 假门）；
- 房间只能**读图纸标注**，不能由墙拓扑自动分格。

已知残留病灶几乎都能追到这里：`~1.5m 楼梯井碎片`（c056/c057/c059-065）、
`c022 粗环/斜墙`、`内隔墙 1-3m 注册漂移`（审计排除了部分假阳性，但真病例仍需引擎级修）。

### 问题 4：图纸语义只用了一半

中文建筑 CAD 有强约定（GB/T 50104）：
- **线宽**：粗线 = 剖切到的墙/柱，细线 = 投影线，点划线 = 轴线；
- **线型/图例**：涂黑 = 钢筋混凝土楼板，斜线填充 = 砖。

现在**资产图不携带线宽**，只能退化成几何规则（间距阈值、形状判据）——
这就是为什么 `component_library.py` 越写越厚、`arch-rules-over-geometry` 只能当原则而落不了地。
**轴线网（点划线）现在完全没用**：它本可以作为定位骨架，把墙挂靠上去，
一次性解决 1-3m 的注册漂移。

### 问题 5：质检有门禁、没有基线对比

`qa_structural.py` 的 I1–I10 不变量做得很好（只读存档 JSON，不碰生产数据），
但它只回答"这栋楼现在合不合格"，不回答"这次改动让它**变差了吗**"。
本次图纸回归事故（`python _dxf_png_batch.py c006` 把 48 栋总目录覆盖成 1 条、
并把忠实源图纸顶成叠加图）**没有任何门禁能拦住**，是用户肉眼发现的。

---

## 三、下一阶段规划

总目标一句话：**从「图元分类 + 几何后处理」转向「建筑对象模型」。**
墙是对象、洞是墙上的参数、房间由墙拓扑推导——这是从"画得像"到"建模对"的跃迁。

### 阶段一：收敛（消除断裂与特例）— 优先级最高

1. **统一构件读取层**：把 `classify` / `classify_line` 合并成一个"图元 → 构件"层，
   按**图层 + 块引用语义**解析，不按图元类型分叉。`INCLUDE_INSERT` 之类差异下沉到
   `BuildingProfile` 的声明式参数，删掉代码里的 `if classifier == "line"`。
2. **单一"墙的真相"阶段**：把 `recognize` 的内墙 + `_wall_thin_*` 的重建合并成
   一个函数，产出**符合 schema 的墙列表**（必有 `id`/`type`/`thickness`/`height`/`poly`/`holes`），
   门洞挖穿成为这个阶段的内建步骤，而不是外挂的 `_punch_doors`。
3. **特例开关声明式化**：`pair_curved` / `outline_unify` / `door_by_points` 全部
   变成 `profile.json` 里的参数，代码里不再出现楼名或楼相关分支。
4. **一次性脚本归档**：`_*.py` 移入 `_scratch/`，只保留管线入口。
   交付命令固化为**一条** `python run_deliver.py <name> [--glb] [--windows]`。

> **验收口**：48 栋用同一条命令、零代码特例重建，`qa_structural` 相对基线**零回归**。

### 阶段二：几何严格化（墙-洞口-房间对象模型）

1. **墙对象三要素**：`轴线（起终点）+ 双皮（厚度）+ 洞口集合`。
   现在的 `pair_wall_faces` 已经能配出厚度，往上补一层轴线即可。
2. **洞口统一模型**：门、窗、洞统一表达为**沿墙轴的一维区间** `(t0, t1, 底标高, 顶标高, 类型)`。
   - 门 = 落地区间，窗 = 离地区间，洞 = 全高区间；
   - 盒子由区间**沿墙轴扫出**，天然不会"窗被当门"、不会深度算错；
   - 渲染层直接读区间补过梁/窗台，不再需要 `bx0..by1` 这种旁路字段。
3. **T/L 接合**：墙轴求交，按交点修剪墙段，消除相交处重叠。
4. **房间自动分格**：用墙拓扑做 planar arrangement（平面图分割），
   与图纸标注的房间号做**匹配校验**，不一致的报出来——而不是反过来直接信标注。
5. **弧墙并入同一模型**：`pair_curved` 从开关升级为轴线模型的一等公民（曲轴 + 双皮）。

> **验收口**：① 内外墙洞口零重叠、零浮空；② 房间自动分格与图纸标注吻合率（先报基线再逐栋提）；
> ③ 残留病灶清单（楼梯井碎片 / 粗环 / 注册漂移）逐项消号。

### 阶段三：语义与标准（更专业的输出）

1. **争取带线宽/线型的图纸源**——这是识别率的天花板，比任何几何阈值都值钱。
   若拿不到，则建**图例形状库**：涂黑实心 → 砼楼板，斜线填充 → 砖墙，
   按"填充图案 + 边界"判构件，比猜间距可靠得多。
2. **启用轴线网**：点划线提取 → 轴网骨架 → 墙按最近轴线挂靠，
   先修注册漂移，再谈其他。
3. **参数化输出**：墙/柱/板带材质与构造做法；输出 **LOD 分级**
   （导航低模 / 展示高模），而不是一个模型打天下；
   具备条件时导出 **IFC**，让成果能进 BIM 工具链。

### 贯穿始终：回归金标准 + 门禁

- **金标准楼**：冻结的 {c006, c009, c103, c104} + 手修过的楼（如 c041 F1、c057）作为回归基准。
- **qa diff 门禁**：每次交付跑 `qa_structural` 并与基线 JSON 对比，任一楼任一不变量恶化即阻断。
- **产物归属声明**：每个渲染/导出脚本在文件头声明**它拥有哪个目录**，
  并加"单栋调试不写总目录"这类保护（`_dxf_cad_render.py` 已有，`_dxf_png_batch.py` 本次补上）。
  —— 2026-09-10 图纸回归事故的直接教训。

---

## 四、立即可做（低成本、高收益）

1. **归档 100+ 个 `_*.py`** 到 `_scratch/`，管线入口收敛——纯整理，零风险，收益立竿见影。
2. **c019 的窗**：目前全是合成窗。按阶段二的"洞口区间"思路补真窗，
   与刚修好的门共享同一模型。
3. **32 个陈旧 GLB 重出**：用 `_glb_only.py`（不重跑 recognize）批量刷新。
4. **13 栋 sw=0（无楼梯井）** 逐栋排查——多为 `#12 墙融合` 埋住，与本次门被埋同源。
5. **ny27-29 人才公寓 3 栋**尚未纳入，补齐即 51 栋。

---

## 五、一句话总结

本次 c019 暴露的不是"一个 bug"，而是**管线没有对象模型**：
门洞盒被一个 `door_by_points` 开关关住、内墙在两个阶段各生成一次、修复只能靠外挂脚本。
**下一步的重点不是继续加判据，而是把墙-洞-房间建成对象**——
判据会随图纸千变万化，对象模型才能让 48 栋楼共用一套逻辑。
