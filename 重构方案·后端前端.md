# gym3d 重构：后端 + 前端（可上服务器）

> **状态**：方案已定稿并批准，2026-09-11 起实施。
> 进度锚点见 git 标签：`pre-refactor`（Phase 0 完成）→ `phase-1` … `phase-6`。
> 每阶段都必须「系统仍可用 + 有回退锚点」，任何一步出问题 `git revert` 单提交即可。
>
> **本文件是这次重构的唯一路线图**：四个已拍板决策（范围=全都要、栈=FastAPI+React/TS、
> 算力=服务器只服务、部署=PM2）、21 条路由的合并表、三道「只服务」防线、7 个阶段与
> 逐阶段验收 / 回退，以及 9 条风险。改动前先读「风险」和「铁律」。

## Context

`D:\gym3d` 是一套 DXF→3D 建筑数字孪生系统：49 栋楼 / 292 层 / 2.8 GB 产物（GLB 占 2.3 GB）。
今天它由三个**手工 stdlib `http.server` 进程**拼成：

- `backend/web/control.py`（8130，710 行手写路由）—— 建模控制台
- `backend/db/serve_rooms.py`（8123，端口写死）—— 房间查询 + 路径规划
- `python -m http.server 8000` —— 静态前端

前端是 4 个散装 HTML（`frontend/*.html`）+ 原生 ES module + importmap，**three.js 版本双轨**（building.html 走 unpkg 0.160.0，control.html 走本地 0.185.1）。

用户要求：**重构出「后台 + 前端」的形态，能在服务器上跑**。已拍板四个决策：

| 决策 | 选择 |
|---|---|
| 范围 | 控制台 + 3D 查看器 + 房间查询/导航，全都要 |
| 技术栈 | 后端 FastAPI，前端 React + TypeScript(strict) + Vite |
| 算力归属 | **服务器只服务**：建模在本机跑，服务器只发产物 |
| 部署 | PM2（Linux） |

**要解决的问题**：把业务逻辑从 stdlib 里剥出来、把散落的硬编码路径/端口/口令收成一处配置、把「本机全功能」与「服务器只读」做成同一套代码的两种形态、并让 2.3 GB 的 GLB 能被可靠地发出去。

**已确认的硬约束（勘察得出，决定方案形状）**：

1. **`console_meta.spec_defaults()` 在导入期 `import build_standard_glb`** → 拖进 trimesh/numpy/mapbox_earcut/shapely。`/api/meta` 是控制台首屏第一个请求，**服务器上不冻结它必然 500、前端白屏**。
2. **`backend/nav/build_path.py` 需要 shapely + numpy + PIL**，且 `DATA/floors`、`FLOOR_H=4.2` 写死 —— 与「服务器不装重依赖」正面冲突，但导航又是要保留的功能。
3. **`control.py` 的 `/floor/<F>.png` 和 `.dxf` 是请求期现算的**（走 matplotlib / ezdxf），服务器上必须改成静态。
4. **`_safe_static` 把整个文件 `f.read()` 进内存**再写 —— 147 MB 的 GLB 并发两人就常驻 ~300 MB。
5. **job 状态是进程内 `JOBS = {}` 字典**，无持久化、无超时、无取消 → 与 PM2/uvicorn 多 worker 天然互斥，且长任务卡死会永久占住「同楼不并发」的锁。
6. **无 git 仓库**；`data/` 2.8 GB 且全量重建 GLB 实测 **448 分钟**；`data/buildings/*/.orig/` 是人工修复的唯一来源。
7. **口令硬编码**：`LIHUA_DB_PASSWORD` 默认 `postgres123`，`postgres` 超级用户直连。
8. 全仓到处是 `D:\gym3d`、`D:\dxf_output` 绝对路径；前端两处写死 `http://localhost:8123`。

**顺带已完成的账**（本方案之外，但同一批工作）：49 栋 GLB 已全部重出，网格开边 0.000%（`_scratch/_check_mesh.py` 全量审计），数据未动。唯一残留 **`data/lihua-building.glb` 仍 29.863% 开边**（理化楼基线，不在 49 栋批次内），需单独重出。

---

## 目标结构

### 后端

```
backend/
├─ api/                                  ★ 新：服务器安全区，模块级零重依赖
│  ├─ main.py            create_app()：lifespan / CORS / 异常处理器 / 挂 routers / 静态挂载
│  ├─ run_api.py         入口 shim，uvicorn.run(workers=1)（供 PM2 用）
│  ├─ settings.py        pydantic-settings，全部路径/端口/口令的唯一来源
│  ├─ deps.py            get_settings / require_compute / require_admin
│  ├─ responses.py       ok()/fail() 统一信封 {success,data,error,meta} + 异常改写
│  ├─ schemas.py         全部 pydantic 模型
│  ├─ routers/           health · meta · buildings · jobs · rooms · nav
│  └─ services/
│     ├─ artifacts.py    ★ 从 control.py 剥离的 ~20 个纯函数（见下）
│     ├─ meta_source.py  读 data/_meta/console_meta.json（冻结），compute=1 才回落实时求值
│     ├─ manifest.py     读写 <building>/manifest.json（sha256 + buildId + stale 归因）
│     ├─ jobs.py         JobManager：冲突规则 / subprocess / 日志 / 超时 / 取消 / 落盘
│     ├─ compute.py      唯一允许 import run_step 与起子进程的模块
│     ├─ rooms_repo.py   psycopg 连接池 + query_rooms
│     └─ path_solver.py  服务器侧 A*（纯 numpy + zlib 掩膜）
├─ web/{console_meta.py, run_step.py}    保留：console_meta 仍是单一事实源，run_step 仍是子进程入口
├─ db/{load_rooms_db.py, schema.sql}     load 改读 settings；schema.sql 新增（现在 DDL 散在代码里）
└─ config/paths.py                       兼容垫片 → settings
```

**`services/artifacts.py` 的移植源**（`control.py` 纯函数，零 http 耦合，逐个搬）：
`_safe_static` `_building_names` `_count_floors` `_floor_png` `_floor_dxf` `_spec_path` `_glb_rel`
`_building_ctx` `_resolve_artifact` `_floors_mtime` `glb_is_stale` `building_status` `list_buildings`
`get_profile`/`put_profile`（**合并写语义是正确性关键**）`get_spec`/`put_spec` `stage_argv` `job_conflict`
要重写的只有 `Handler` 类里的 `do_GET`/`do_PUT`/`do_POST` 与手写头。

### 前端

```
frontend/
├─ package.json  vite.config.ts  tsconfig.json(noUncheckedIndexedAccess)  eslint.config.js
└─ src/
   ├─ main.tsx / app.tsx / router.tsx
   ├─ api/       client.ts · schema.d.ts(openapi-typescript 生成) · types.ts(Zod 收窄)
   ├─ components/ layout · schema-form(通用表单渲染器 + field-registry) · common
   ├─ features/  buildings · console · viewer · rooms
   ├─ three/     ★ 框架无关，禁止 import react
   │             engine · materials · constants · viewer(门面) · loaders · picking · labels
   │             geom/{shape-utils,facade-windows,wall,window,roof,stair,column,floor-builder}
   ├─ stores/viewer-store.ts    Zustand：纯 UI 状态
   └─ styles/
```

**路由**：`/` 楼栋总览 · `/buildings/:name/:floor?` 整栋 3D · `/console/:name?/:stage?` 建模控制台 ·
`/rooms` 房间查询 + 导航 Tab · `/legacy/*` 旧页（过渡期）。

**850 行 three.js 的迁移**（`building.html:101-1155`）—— 纯搬迁，零逻辑改动。三个要点：

1. **`viewer.ts` 是唯一门面**，命令式 API（`setBuilding/setFloor/setWallsVisible/selectByRank/drawPath/on('pick')/dispose`）。
2. **模块级可变全局（`S`/`FLOOR_COUNT`/`LAYER_HEIGHT`/`loadedFloors`/`wallMeshes`/`wallsTransparent`）全部收进 `createViewer` 闭包**。这是唯一有真实回归风险的动作：现状 `renderFloor`/`applyStyle`/`setWalls` 读写顶层 `let`，直接搬进模块作用域会变成跨实例共享的 bug。逐条对照 `grep -n "^let "` 清单，防止漏掉。
3. **`three-canvas.tsx` 是唯一 React↔three 桥**（`useRef<Viewer>` + 两个显式依赖的 `useEffect`）。事件 → Zustand → `info-card` 订阅渲染；**store 不反向驱动几何重建**。保留 `window.__gym3d` 调试钩子供截图回归用。

**状态分工**：服务端状态 → TanStack Query；UI 状态 → Zustand；路由状态 → URL searchParams；表单 → react-hook-form + zodResolver。
job 轮询用 `useQuery({refetchInterval})`，`jobId` 落 sessionStorage + 新增 `GET /api/jobs?active=1`，刷新不丢进度。不用 SSE（PM2 单进程 + nginx 反代下需额外调 `proxy_buffering off`，收益不抵复杂度）。

**表单 schema 驱动**（保持 `console_meta.py` 单一事实源）：Zod 把 `fetch` 的 `unknown` 收窄成 `MetaField`（**同时满足「禁 any」**）→ `field-registry.tsx` 单一分派表 → `SchemaForm`。
`control.js` 的 `applyField`/`parseList`（`control.js:44-59`）转换语义**逐条照搬并写单测** —— 「`glb_windows=false` 不能被当假值删掉」「清空字段传 null = 显式关闭」的落点。

---

## 配置与路径解耦

`backend/api/settings.py` 为唯一来源：`ROOT = Path(__file__).resolve().parents[2]`，**代码里不出现任何盘符**。

`.env.example` 键清单：`GYM3D_ROOT` `GYM3D_DATA_DIR` `GYM3D_DXF_DIR` `GYM3D_FRONTEND_DIST`
`GYM3D_COMPUTE` `GYM3D_ENV` `GYM3D_HOST` `GYM3D_PORT` `GYM3D_API_PREFIX` `GYM3D_CORS_ORIGINS` `GYM3D_LOG_LEVEL`
`LIHUA_DB_{HOST,PORT,USER,NAME,PASSWORD,REQUIRED}` `GYM3D_JOB_{TIMEOUT_S,MAX_CONCURRENCY,PERSIST}`
`DEEPSEEK_API_KEY` `GYM3D_ADMIN_TOKEN`。

两条 validator（对齐全局规范「拒绝启动」）：`env=prod 且 db_required 且 db_password 为空` → raise；
`db_password` **删掉 `postgres123` 默认值**。

| | Windows 本机 | Linux 服务器 |
|---|---|---|
| `GYM3D_COMPUTE` | `1` | `0` |
| `GYM3D_DXF_DIR` | `D:/dxf_output` | 不设 |
| 值来源 | `D:\gym3d\.env` | `/opt/gym3d/.env`（chmod 600），**不进 ecosystem.config.js** |

**三处路径整改**：① 删所有 `sys.path.insert(0, r"D:\gym3d")` → 用 `settings.root`；② `os.path.commonpath` → `PurePath.is_relative_to`（Windows 跨盘抛 `ValueError` 靠 try 兜底，Linux 不抛 → **语义会静默改变**）；③ `profile.json` 里的 `dxf`/`out_dir` 改存相对路径，读时由 settings 拼接，配一次性迁移脚本 + 逐楼 diff 校验。

---

## 「只服务」模式

**单代码库 + 单 App + 三道防线**（不用两套代码——读接口占 80%，拆开必漂移）：

| 层 | 机制 | 拦不住时 |
|---|---|---|
| 依赖层（最硬） | 服务器 venv 只装 `requirements-server.txt`，**物理上没有 ezdxf/shapely/trimesh** | 无法绕过 |
| 能力层 | `GYM3D_COMPUTE=0` → `require_compute` 依赖注入 → **403** | 无法绕过 |
| 前端层 | `GET /api/capabilities` → 隐藏运行按钮 | 只是 UI，不承担安全 |

用 **router 级依赖**（`APIRouter(dependencies=[Depends(require_compute)])`）而非逐路由装饰器 —— 漏一个就是漏洞。

**403 而非 404/501**：语义准确且与「资源不存在(404)」可区分，前端靠 `detail.code === 'compute_disabled'` 精确分支。

`GET /api/capabilities`（在 require_compute 之外）返回 `{compute, env, features:{runStage,editProfile,editSpec,floorPng,floorDxf,rooms,path}}`。前端在 `app.tsx` 注入 `CapabilityContext`，**只暴露语义化开关**（`canRunStage`/`canEditProfile`/`canDownloadDxf`），组件问语义不问裸 flag。

`compute=0` 时 `/api/meta` 对 PIPELINE 做**字段白名单投影**：去掉 `script`/`args`，`runnable` 置空 —— 阶段表照常显示「做过没有」，只是没有运行按钮，**零分叉渲染逻辑**。

---

## API（合并后）

统一信封 `{success, data, error, meta}`；二进制与 SPA 例外。

| 方法 | 路径 | 旧接口 | compute |
|---|---|---|---|
| GET | `/api/health` `/api/capabilities` | 新 | — |
| GET | `/api/meta` | control | 裁剪 |
| GET | `/api/buildings` | control | 否 |
| GET | `/api/buildings/{n}/status` | control | 否 |
| GET | `/api/buildings/{n}/floors` | **新** | 否 |
| GET/PUT | `/api/buildings/{n}/profile` | control | PUT **403** |
| GET/PUT | `/api/buildings/{n}/spec` | control | PUT **403** |
| POST | `/api/buildings/{n}/run` | control | **403** |
| GET | `/api/jobs/{id}` · `/api/jobs?active=1` | control（+新） | **403** |
| POST | `/api/jobs/{id}/cancel` | **新** | **403** |
| GET | `/api/buildings/{n}/floor/{f}/plan.png` | control | 否（静态） |
| GET | `/api/buildings/{n}/floor/{f}/source.dxf` | control | **403** |
| GET | `/api/rooms` `/api/rooms/facets` | serve_rooms（+新） | 否 |
| GET | `/api/path` | serve_rooms | 否（见风险 3） |
| GET | `/data/{path}` · `/{path}` | 两者 | 否 |

行为差异（有意为之）：错误码细化为 403/404/409/422；`RequestValidationError` 改写进统一信封（否则前端收到 FastAPI 原生 `{detail:[...]}` 解析失败）；`{name}` 统一用 `Path(pattern=r"^[A-Za-z0-9_-]+$")`；**迁移期保留 `/legacy/*` 四个旧页**（可回退的关键）。

---

## 大文件与产物

**GLB（2.3 GB）→ nginx `sendfile` + `aio threads` 直发，FastAPI 完全不碰**。Starlette `FileResponse` 虽支持 Range，但 147 MB 过 ASGI 会拖垮单 worker；nginx 是零拷贝且 Range 白送。**不要对 GLB 开 gzip**（已压缩，且会破坏 Range）。
dev 无 nginx 时用 `StaticFiles`，但**顺手把 `_safe_static` 的全量 `f.read()` 换成 `FileResponse`**（现服务的真实缺陷）。

**floors JSON**：按需 `useFloor(building, f)` + `staleTime: Infinity`，切层保留上一层几何不闪白；渲染完 200ms 空闲预取相邻 ±1 层；HTTP 缓存靠 nginx `open_file_cache` + ETag（文件名要稳定，不能嵌 hash）。新增 `/floors` 返回层清单，**前端不再假设 `0..count-1` 连续**。

**楼层 PNG**：`scripts/prerender-floor-png.py` 构建期预渲染到 `plans/recog_floorN.png`，服务器纯静态发 → `compute=0` 时该功能仍可用，且**顺带干掉无界增长的 `_FLOOR_CACHE`**。另出 `plans/thumb.webp`（400px 首层）给总览网格用。

**可选（独立，不进主链路）**：GLB 用 `trimesh` 的 quadric decimation 或 Draco 压到 15–25 MB/栋，2.3 GB → ~300 MB，同步从小时级降到分钟级。代价是前端要挂 `DRACOLoader`。

---

## 实施路线（每阶段系统仍可用，每阶段有回退锚点）

**Phase 0 —— 保底与冻结**（零行为变更）
`git init` + `.gitignore`（`data/` `venv/` `node_modules/` `dist/` `.env` `__pycache__/` `*.bak_*` `*.orig_*`）+ 首个 commit + **tag `pre-refactor`**（整个计划的安全绳，因为现在无 git）。拆 `requirements-server.txt` / `requirements-compute.txt`（当前零依赖清单文件）。
*验收*：`git status` 干净；两个老服务行为与改造前完全一致。*回退*：`rm -rf .git`。

**Phase 1 —— 配置与路径解耦**（后端仍是 stdlib，前端不动）
落 `settings.py`；四个脚本改读 settings；档案相对化 + 迁移脚本；`freeze-meta.py` 出 `data/_meta/console_meta.json`；首次全量预渲染。
*验收*：`grep -rn "D:\\\\gym3d\|localhost:8123\|8123" backend/ frontend/` **零命中**；改 `GYM3D_PORT=8131` 服务正常；**干净 server-only venv 里 `import backend.api.settings` 成功**。*回退*：`git revert` 单提交。

**Phase 2 —— FastAPI 骨架 + 双跑对拍**
`backend/api/**` 起在 **8140**（8130 留给老 control.py，前端无感）。写对拍脚本并发打两个端口的只读接口做 JSON diff（忽略 mtime 类）。job 能力补**超时 + 取消 + 落盘 `data/_jobs/<id>.jsonl`**。
*验收*：对拍全绿；`run` 能跑完并轮询到 done；`cancel` 能杀长任务；进程重启后 `?active=1` 能报出孤儿 job。*回退*：关掉 8140，8130 从未动过。

**Phase 3 —— 前端脚手架 + 控制台迁移**
Vite + React + TS strict + Query + Router + Zustand；proxy `/api`、`/data` → 8140。迁 `/console`。
*验收*（对照清单逐条勾）：楼栋列表 / 选中 / profile 全字段读写 / spec + style 颜色 / 阶段表 done-stale-inplace 三态 / 运行 + 409 冲突提示 / 日志轮询 / GLB 预览 / `glb_windows` 勾选提示。老 control.html 仍在 8130 可用。*回退*：不部署 dist。

**Phase 4 —— 3D 查看器迁移（风险最高）**
先抽 `src/three/**`（纯搬迁零改动），再做 viewer-page。**three.js 此阶段锁死 0.160.0**，与 building.html 完全同版 —— 把「框架迁移」和「版本升级」彻底解耦。建 Playwright 视觉回归基线（固定相机/楼层/像素比，对 `/legacy/building.html` vs `/buildings/c006?floor=3` 截图，选 c006 有过渡层坡顶 / c019 高层 / lihua 基线三栋）。
*验收*：3 栋 × 2 层像素差 < 0.5%；墙体显隐/透明/窗开关/拾取/构件描述/`__gym3d` 全可用。*回退*：`/legacy/building.html` 永不删。

**Phase 5 —— 房间查询 / 导航页**
接 Postgres（连接池）；路径 A* 按风险 3 的方案 A 落地。
*验收*：`/api/rooms?building=c006&floor=2` 与老 8123 逐行一致；`/api/path` 折线点数与端点一致。*回退*：老 8123 仍在。

**Phase 6 —— 只服务 + 上线**
nginx + PM2 + 同步脚本；`GYM3D_COMPUTE=0`。
*验收*（服务器 curl 逐条）：`curl -r 0-1023` 返回 **206**；`POST .../run` → **403 compute_disabled**；`/api/meta` 响应**不含 `script`/`args`**；服务器 venv 上 `python -c "import ezdxf"` **ImportError**（证明依赖隔离生效）。
**★ 下线时刻**：Phase 6 全绿 + 服务器观察 24h 无 5xx 后，才删 `control.py`/`serve_rooms.py`/旧 HTML。**不要提前删** —— 它们同时是回退路径和在线参考实现。

**Phase 7（可选，独立）—— three.js 版本统一**
`three@0.160.0` → `0.185.1` 单独一个 PR，**必须重跑 Phase 4 截图基线**。已知要核：r155+ 光照单位变化（`useLegacyLights` 默认翻转 → `sun.intensity=2.6` 会明显过曝）、`RoomEnvironment` 构造签名、`outputColorSpace`。不通过就**保持 0.160.0 并删掉 `node_modules/three` 里手放的 0.185.1** —— 双轨比旧版本更危险（同页两套 three 会让 `instanceof THREE.Mesh` 失效，拾取静默失灵）。

---

## 部署

**PM2**（`ecosystem.config.js`）：`instances: 1` + `exec_mode: 'fork'` —— **必须**，job 状态在进程内，cluster/多 worker 会让轮询 404（唯一正确解是引入 Redis，当前规模不需要，记为技术债）。`max_memory_restart: 600M`（只服务模式内存很小，超了说明泄漏）。口令**不放这里**（该文件进 git），走 `/opt/gym3d/.env`。

**nginx**：`root /opt/gym3d/frontend/dist` + SPA fallback；`/assets/` immutable（Vite 带 hash），`index.html` no-cache；`/api/` 反代 8130（`proxy_read_timeout 300s`）；`/data/` alias + `sendfile` + Range。

**鉴权**（推荐分级）：`/`、`/assets/`、`/data/**` **公开**（3D 展示就是给校庆/参观用的，加登录失去意义）；`/console` 与**写路由** → nginx HTTP Basic（`htpasswd -B` bcrypt，零代码）。不做 JWT —— 没有用户体系，刷新/登出/存储全套代价换不来收益。DB 建**只读账号** `gym3d_ro`，Postgres 只监听 127.0.0.1。

**数据同步**（2.8 GB）：`rsync --checksum --partial --append-verify`，**默认禁用 `--delete`**（`.orig/` 是人工修复唯一来源）。首次 2.3 GB 建议夜间 `--bwlimit` 或走移动硬盘。**同步前 `pm2 stop`，同步后 `start`** —— 否则用户会拉到写了一半的 GLB。配 `make-manifest.py`（每栋 sha256 + `buildId`）+ `verify-sync.py` 作为同步的**强制末步门禁**。

---

## 风险

| # | 风险 | 缓解 |
|---|---|---|
| 1 ★★★ | **three.js 双轨 0.160↔0.185**：r155+ 光照单位变更会让 `sun.intensity=2.6` 整体过曝 | Phase 4 锁死 0.160.0，版本升级独立成 Phase 7；用像素差而非肉眼判；`package.json` 用精确版本不加 `^` |
| 2 ★★★ | **Windows→Linux 路径**：`D:\gym3d` 字面量、49 份绝对路径、`commonpath` 跨盘语义差异、CRLF | Phase 1 grep 门禁；`is_relative_to` 全替换；`.gitattributes` 加 `eol=lf`；Linux 容器冒烟 `import backend.api.main` 必须不 import ezdxf |
| 3 ★★★ | **`/api/path` 需 shapely 与「不装重依赖」正面冲突** | **方案 A（推荐）**：构建期用 shapely 把每层栅格化成 1-bit 通行掩膜，zlib 压缩存 `nav/floorN.mask.bin`（292 层合计 <15 MB），服务器只做 `zlib.decompress` + **纯 numpy A\***。代价：`RES=0.1`/`FLOOR_H` 被冻进掩膜，改分辨率需重跑。**方案 B（保守）**：`/api/path` 标 compute-only 返回 403，前端降级提示。两方案都**顺手修掉 `build_path.py` 的 `DATA/floors` 与 `FLOOR_H=4.2` 硬编码** |
| 4 ★★ | **Postgres 口令硬编码** `postgres123` + 超级用户直连 | 删默认值 + prod validator 拒绝启动；只读账号；`.env` 进 `.gitignore`；旧口令**视为已泄露并轮换**（改口令，不是删提交） |
| 5 ★★ | **数据同步不一致**（GLB 与上游 floors 不同步；mtime 判据已知不可信——c022 活例） | `manifest.json` 带 sha256 + `buildId`；`status` 返回 `staleReason`；前端卡片显示产物版本 + 过期徽标；`verify-sync.py` 强制门禁 |
| 6 ★★ | **无 git → 回退手段为零**，`data/` 448 分钟才能重建 | Phase 0 立刻 `git init` + tag；`data/` 不进 git 但服务器副本 + 移动硬盘冷备；每阶段一个 tag |
| 7 ★★ | **job 状态在进程内 + PM2 重启即丢**，25 分钟的 recognize 跑一半消失 | 日志 append 到 `data/_jobs/<id>.jsonl`，启动时扫描孤儿标 `interrupted`；`?active=1` 让前端恢复。**同时补上现状完全没有的超时与取消**（现在卡死的阶段会永久占锁） |
| 8 ★★ | **`/api/meta` 的重依赖打穿只服务模式**（首屏白屏） | Phase 1 就冻结 `console_meta.json`；动态求值只在 `compute=1` 时发生；`freeze-meta.py` 里做一次 `specDefaults` 键集校验（把 `self_check()` 前移到构建期） |
| 9 ★ | **`_FLOOR_CACHE` 无界增长 / `_safe_static` 全量读入内存** | 改 `FileResponse`（Phase 2）；预渲染落地后删掉缓存；`max_memory_restart` 配告警 |

---

## 验证

**每阶段**：`npm run check`（lint + typecheck）/ `git tag phase-N` / 上一阶段验收清单复跑一遍（防回归）。

**数值验证（不是类型检查）**：
- 后端：`curl` 逐条打 21 个路由核状态码与信封；对拍 8130 vs 8140 的 JSON diff。
- 前端：Playwright 视觉回归，像素差 < 0.5%。
- 只服务模式：服务器上 `curl -r 0-1023` 得 206、`POST /run` 得 403、`/api/meta` 无 `script` 字段、`import ezdxf` 得 ImportError。
- 数据完整性：`verify-sync.py` 按 manifest 逐文件核 sha256，**非零退出即同步失败**。

**本批已完成、可作为 Phase 0 起点的既有资产**：`_scratch/_check_mesh.py`（GLB 闭合审计）、`_rebuild_guarded.py`（带数据指纹守卫的重出）、`_verify_stairs_all.py`、`_vol_oracle.py`（材料体积独立对账）。这些进 `scripts/` 或保留在 `_scratch/`，作为重构期间「产物没被我改坏」的对照基线。

**遗留待办**：`data/lihua-building.glb` 仍 29.863% 开边，需单独重出（理化楼是注册表楼，走 `bsg.DATA=data, bsg.OUT=lihua-building.glb` 那条路径，不在 49 栋批次内）。
