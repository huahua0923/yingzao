# 建模侧依赖 —— **只装在本机**，服务器绝不安装。
#
# 这些包负责 DXF 解析、几何运算、网格生成。版本一律用 == 精确锁定：
# 换一个 shapely / mapbox-earcut / trimesh 版本，三角剖分与布尔运算的结果就可能变，
# 49 栋楼的 GLB 会跟着变 —— 那属于「重算」而不是「重构」，不能在重构期间混进来。
#
# 装法（本机）：
#     python -m venv venv && venv/Scripts/pip install -r requirements-compute.txt
#
# 已在本机验证的版本来源：Python 3.12.10 / Windows；下方 == 号后的版本号
# 就是产出当前 49 栋 GLB 的那一套，原样抄录，不做升级。

# ── DXF 读写与审计 ──────────────────────────────────────────────
ezdxf==1.4.4
lxml==6.1.1                  # ezdxf 的可选加速/XML 后端

# ── 几何运算（墙体外皮配对、布尔、偏移）─────────────────────────
shapely==2.1.2
numpy==2.5.1

# ── 网格生成与导出 ──────────────────────────────────────────────
trimesh==5.1.0
mapbox-earcut==2.0.0         # 挖洞三角剖分。**不能换成 shapely.ops.triangulate**
                             # —— 后者是 Delaunay 覆盖凸包，会把门洞填平。

# ── 平面图渲染（每层 PNG 比对、识别色卡）────────────────────────
matplotlib==3.11.1
Pillow==12.2.0

# ── 房间数据入库 ────────────────────────────────────────────────
psycopg[binary]==3.3.5

# ── 拉取/上传（校庆系统接口、CDN）───────────────────────────────
requests==2.34.2

# ── 视觉回归基线（Phase 4 用，只在本机跑）───────────────────────
playwright>=1.40

# 说明：
#  * bpy（Blender 5.2.1）不走 pip，是独立安装的渲染后端，见 blender-headless-render 笔记。
#  * DEEPSEEK_API_KEY 等密钥只在 .env，不在这里，也不在代码里。
