# ══════════════════════════════════════════════════════════════════
# gym3d 忽略规则
#
# 原则：git 只管「代码 + 配置 + 文档」。凡是可以重跑的产物、可以重新下载的
# 依赖、以及含密钥的本地配置，一律不进版本库。
#
# 本仓库总体积 4.6 GB，其中 4.5 GB 是产物与依赖 —— 加进来会让 git 直接不可用。
# ══════════════════════════════════════════════════════════════════

# ── 产物数据（2.75 GB）────────────────────────────────────────────
# floors/*.json、*.glb、plans/、dxf_plan/、rooms.json、profile.json、spec.json
# 全在这里。注意：data/buildings/*/.orig/ 是人工修复成果的唯一来源，
# 排除出 git 之后**必须靠机外备份（服务器副本 + 移动硬盘）保命**，见 README。
data/

# ── GLB 备份与临时产物 ────────────────────────────────────────────
_backup_glb_20260910/
_tmp_c019_door/
*.glb

# ── _scratch 里的大件（脚本要留，GLB/图不要）──────────────────────
# _scratch/ 下 244 个 .py 是验证工具链（闭合审计/守卫重建/楼梯体检），必须留。
# 但这三个子目录是历史探针产物，占 1.1 GB。
_scratch/legacy/
_scratch/_before_regionfix/
_scratch/win_probe/
_scratch/tmp/
_scratch/probe-out/

# ── 依赖与构建产物 ────────────────────────────────────────────────
node_modules/
venv/
.venv/
env/
# 不带尾斜杠：尾斜杠只匹配「已存在的目录」，对还没构建出来的 dist 形同虚设
frontend/dist
dist
__pycache__/
*.pyc
*.pyo
*.egg-info/

# ── 密钥与本地配置（铁律：密码/密钥只在 .env）────────────────────
.env
.env.*
!.env.example

# ── 引擎备份文件（git 接管历史后冗余，但**不要删**，见 README）──
*.bak
*.bak2*
*.bak_*
# 用 *.orig* 而不是 *.orig_*：库里还有 .orig2 / .orig_engine 这类变体
*.orig*
.orig_patch/
*.before_xrange

# ── 日志 ──────────────────────────────────────────────────────────
*.log
logs/

# ── 提交信息草稿（git log 已留档，不必进库）──────────────────────
_scratch/_commitmsg_*.txt

# ── 编辑器 / 操作系统 ─────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store
