# gym3d 算法与流程总览

> 面向：接手本项目的工程师 / 未来的自己
> 依据：2026-09-10 对 `backend/` 全量代码逐文件通读后的整理（5 组并行梳理）
> 配套：`三维建模·算法总结与下一阶段规划.md`（诊断与规划）、`建模方法论·现状·缺口·专业优化路线.md`（现状与缺口）、`kb/`（CAD 识图知识库）
> 规模：48 栋 / 292 层 / 48 GLB

---

## 0. 怎么读这份文档

- **只想跑一遍** → 直接看 §6「标准操作序列」。
- **想知道某个数从哪来** → 看 §3「阈值总表」。
- **想改算法** → 先看 §4「全局不变量」（改错会静默毁数据），再看 §5「已知局限」。
- **想找某段代码** → §7「脚本索引」。

本项目的特殊性：**模型是纯文本 JSON + GLB，所有判断必须靠几何/数值**。截图看不出来，肉眼看不出，唯一的裁判是脚本。因此本文档里每个阈值都写了它在防什么。

---

## 1. 全链路一张图

```
   DWG 源图纸
      │  convert_dwg_to_dxf.py          (§2.1)  ODA File Converter 27.1.0，audit=1
      ▼
   DXF (CAD 毫米坐标, 图层 + 实体)
      │
      ├─────────────────────────────┐
      │                             │
      ▼                             ▼
   profile.json                  classify / classify_line   (§2.3)
   (是否 line 约定？)              ↓
   BuildingProfile               walls / doors / stairs / columns
      │                             │
      └──────────────┬──────────────┘
                     ▼
              extract_floor()        (§2.5)  22 步：配对 → 推导 → 组装 → 门 → 房间
                     │                        调用 geometry.py (§2.4)
                     ▼
              floor{N}.json  (标准 schema §4.1)
                     │
                     │  recognize() 产的墙是"粗环 blob"，不是交付物
                     ▼
              _wall_thin_batch.py    (§2.6)  从 DXF 重配对 → 真实厚内墙
                     │  + _punch_doors()     门洞在这里也必须挖一次
                     ▼
              floor{N}.json  (终态)
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
   qa_structural  build_standard_glb  _dxf_cad_render
   I1-I10 门禁     (§2.7) → .glb      (§2.9) → 源图纸 PNG
                                        _dxf_png_batch → 识别叠加图
                                        _dxf_audit → 覆盖度审计
        │
        ▼
   data/buildings/index.json → 网页选择器 / serve_rooms.py (§2.10)
```

**一句话记忆**：DXF 进来 → 分类器切成四类几何 → 按楼层组装成 JSON → **内墙二次重建** → 挖门洞 → 出门禁 → 出 GLB 和图纸。

---

## 2. 分层详解

### 2.1 输入层：DWG → DXF

`convert_dwg_to_dxf.py`

| 项 | 值 |
|---|---|
| 引擎 | ODA File Converter 27.1.0 |
| 关键参数 | `audit=1`（**铁律**，不开会漏实体） |
| GUI | 必须 `SW_HIDE`，无头运行 |
| 模式 | 增量转换（已存在的跳过）+ ezdxf 校验 |

DWG 是二进制，ezdxf 读不了。这一步是整个管线的地基，audit 不开会出现「图纸看着全，实体少一半」的诡异缺件。

### 2.2 配置层：BuildingProfile

`backend/recognizer/profile.py` + `data/buildings/<name>/profile.json`

核心字段：

| 字段 | 含义 | 备注 |
|---|---|---|
| `classifier` | `"lwpolyline"`（默认）或 `"line"` | **唯一的分类器分派开关**，见 §2.3 |
| `door_by_points` | 是否走"点对"门判据 | 仅理化楼为 `True`，其余走 `detect_doors` |
| `floor_ys` / `floor_plans` | 楼层标高分带 / 逐层轮廓基准 | 决定 `floor_of()` 的归属 |
| `outline_unify_floors` | 轮廓统层 | 只统一裙楼；退台楼误用会让墙坍塌 |
| `pair_curved` | 弧墙配对开关 | 默认关，开则走 `pair_curved_faces` |
| `cx` / `cy` | 局部坐标原点 | `to_local()` 的基准 |
| `TOWER_SHIFT` | 塔楼相对裙楼偏移 | 阶梯状建筑（如 c006）专用 |

**坐标数学**（`to_local` / `floor_of`）：所有几何先减去 `(cx, cy)` 落到局部米制坐标，再按 `floor_ys` 分带归属楼层。**楼层标签内部从 0 开始**（`floor0.json` = 首层），**显示时 +1**（F1 = 首层）。这个错位在历史上反复咬人。

### 2.3 分类层：两个分类器

`backend/recognizer/classify.py::classify()` — LWPOLYLINE 基线，**所有非 c006 建筑**
`backend/recognizer/classify_line.py::classify_line()` — LINE+INSERT 约定，**仅 c006 逸夫楼**

分派方式（在 recognize 内）：

```python
if getattr(p, "classifier", "lwpolyline") == "line":
    classify_line(...)
else:
    classify(...)
```

两者返回**同构四元组** `(walls, doors, stairs, columns_raw)`，下游无感。

| 维度 | classify (LWPOLYLINE) | classify_line (LINE+INSERT) |
|---|---|---|
| 墙来源 | 闭合多段线 | 双线配对（LINE 成对） |
| 墙厚 | 多段线自身宽度 | 从双线间距读出（240/270/350/380mm） |
| 柱 | 独立图层矩形 | INSERT 块，`scale` 编码真实尺寸（600mm） |
| 门宽 | 几何量取（700–1500） | INSERT 块 scale 解码 |
| 窗 | 图块 240 | 图块 240 |

**关键常量**（两个文件共用语义）：

| 常量 | 值 | 作用 |
|---|---|---|
| `CURVE_MIN_R` | 2.0 m | 小于此半径不算弧墙 |
| `CURVE_STEP_MM` | 250.0 | 弧离散化步长 |
| `SLAB_RATIO` | 1.0 | 面积比 ≥ 此值判为楼板（剔除） |
| `DOOR_LEAF_MIN/MAX` | 0.6 / 3.0 m | 门扇长度区间 |
| `DOOR_SPAN_MAX` | 3.0 m | 门洞最大跨度 |
| `DOOR_JAMB_MIN/MAX` | 0.10 / 0.40 m | 门垛宽度区间 |
| `DOOR_SYMBOL_SPAN_MAX` | 3.0 m | 门符号最大跨度 |
| `DOOR_SYMBOL_TOL` | 1e-6 | 门符号端点容差 |
| `JAMB_LEN/W` | 0.34 / 0.24 m | 门垛矩形尺寸 |
| `INNER_WALL_CENTROID_FACTOR` | 0.75 | 内墙质心判据系数 |
| `FLOOR_CLUSTER_GAP` | 30000 mm | 楼层聚类间隔（30 m 断带） |

`backend/recognizer/component_library.py` 是**识别经验的固化库**：门宽取最长段、slab 面积比、楼层聚类 gap 等都被收进这里。规则是「**识别错就更新库**」。

### 2.4 几何层：配对与推导

`backend/recognizer/geometry.py`

两个核心算法：

**`pair_wall_faces()` — 双皮配对（主路径）**

把两条平行的墙"皮线"配成一面真实厚度的墙。

| 参数 | 值 | 防什么 |
|---|---|---|
| `wall_min` | 0.08 m | 小于此不是墙 |
| `wall_max` | 0.35 m | 大于此是粗环/巨块不是墙 |
| 重叠判据 | overlap > 0.3 | 端点虚接触不算配成 |

配对前必须先做**预处理剔除**，否则会把楼梯踏步线配成假墙（历史 bug：0.3m / 0.12m 假墙塞满楼梯井）：
- 同 y 聚类
- 对称三线剔除

**内皮线过滤**：用**中点距离**而非最小距离——最小距离会误杀垂直隔墙（历史 bug：每层内墙少一半，45→68 / 49→67）。

**`derive_walls_and_outline()` — 三条路径**

按输入形态分派：闭合环 / 双线 / 混合。产出外墙轮廓 + 内墙集合。

**弧墙**（`curve_walls.py`）
- `pair_arc_bands()` — 弧带配对
- `pair_curved_faces()` — 弧面配对，**方向无关**

两道门槛：① classify 阶段弧离散；② curve_walls 方向无关配对。`pair_curved` 开关控制是否启用。

实测识别率：c006 **78.2%** / c009 **74.8%** / c041 **73.4%** / c072·c073 **100%** / c103 41.6% / c104 **86.0%**。

> 反直觉坑：对 c009 关掉 `outline_unify` 会让墙数从 37 坍塌到 121（ERROR）。统层与弧配对是耦合的。

### 2.5 楼层组装层：extract_floor

`backend/recognizer/floor.py::extract_floor()` — **22 步**

主干顺序：

1. 实体过滤（图层 / 类型 / 范围）
2. 楼板剔除（`SLAB_RATIO`）
3. 墙候选配对（`pair_wall_faces`）
4. 楼梯井识别
5. 柱提取
6. 轮廓推导（`derive_walls_and_outline`）
7. 外墙 / 内墙 / 女儿墙分类
8. **门判据（两条路径）**
9. **门洞打穿**
10. 房间提取
11. 窗归属（`wallId` 绑定）
12. 组装 JSON

**门的两条判据路径**：

| 路径 | 触发 | 适用 |
|---|---|---|
| `door_by_points` | `getattr(p,"door_by_points",False)` | 仅理化楼 |
| `detect_doors` | 默认 | 其余全部（LWPOLYLINE 楼） |

**`_hinge_leaf()` — 新门判据（关键）**
判据：两条**轴对齐**、长度相等（相对差 ≤5%）、**互相垂直**、**共享一个铰链端点**的线段。
取代旧的「最长线段 ∈ [0.6, 3.0)」——旧规则把墙垛和窗户矩形全当门，c019 首层 87 扇门里 **86 扇是假的**。

**`_door_boxes()` — 门洞盒（共享）**
供两条判据共用。已用 4000 例随机等价性验证（0 不符）。

> **核心几何原理：门 = gap，不是 hole。**
> 门洞盒深度取 `max(外墙厚, 内墙厚) + 0.05`，**比墙厚更深**。这样 `difference` 后墙被切成**两段**，而不是留一个封闭的洞。
> 深度小于墙厚会出「洞中洞」，布尔运算不稳定，渲染层也没法补门头过梁。
> 渲染层用 `bx0..by1` 恢复门头过梁。

**历史 bug（c019）**：整个门洞盒代码块被 `getattr(p,"door_by_points",False)` 关住，LWPOLYLINE 楼从来没挖过门洞——墙连续、门板埋在实心墙里。

### 2.6 内墙重建层（**交付链的真正一环**）

`recognize()` 产的内墙是**一个粗环 blob**（`is_blob_floor` 判据：覆盖率 > 20%）。它**不是交付物**。

真正的内墙来自：

- `_wall_thin_batch.py` — 批量生产
- `_wall_thin_force.py` — 强制单栋

二者调用 `G.pair_wall_faces` 从 DXF 重建 `type=="inner"` 的墙。

**`_punch_doors(inner_walls, fl)`** — 门洞必须在这里**再挖一次**。原因：`recognize()` 挖的洞会被重建冲掉。**门洞必须在两处都实现**，这是最容易漏的一环。

> `_wall_thin_batch.py` 重建的内墙**没有 `id`**（schema 违约）→ `build_standard_glb.build_walls:149` 直接 `KeyError:'id'`。
> 且门洞把一片墙切成多块后若各块共用同一 id，**窗会被复制到每个碎片上**。
> 故 `punch()` 用 id 分配器：**最大块继承原 id**（保住 `windows[].wallId` 归属），其余取 `x{n}`。

`FROZEN = {"c006", "c009", "c103", "c104"}` — 冻结四栋不得批量重建。

### 2.7 建模输出层

`backend/modeling/build_standard_glb.py`

- `build_walls()` **第 149 行**：要求 `w["id"]` 与 `windows[].wallId` 匹配——**schema 的强制执行点**。
- **双皮墙三段挤出**：墙拆成三段，中间段留窗洞。
- **门头过梁**：为什么 `bx0..by1` 不能用（需单独补盒）。

**`INCLUDE_SYNTHETIC_WINDOWS` 的默认值分裂**（坑）：

| 调用方 | 默认 |
|---|---|
| 模块本身 | `True` |
| `run_building.py --glb` | `False`（除非加 `--windows`） |
| `web/run_step.py` | `False` |
| `_glb_only.py` | 显式设置以对齐 `run_building` |

当前**所有 floor JSON 的窗都是 `synthetic=True`**。重出 GLB 必须用 `_glb_only.py`（不重跑 recognize），并显式对齐这个开关，否则窗会凭空多/少。

GLB 规格跨度极大：14 MB（c006/c017）→ 124 MB（c072/c073）。

### 2.8 质检门禁

**`verify_floor()` — 五阈值**（在 `floor.py` 内）

| 项 | 阈值 | 含义 |
|---|---|---|
| A | > 8 条墙 | 墙太少 = 识别失败 |
| B | 覆盖率 ≤ 20% | 墙覆盖面积占比上限 |
| C | 无墙板面积 ≤ 40% | 板里没墙不能太多 |
| D | 门 ≥ 85% 靠近墙 | 门必须在墙上 |
| E | 房间 ∩ 墙 ≤ 10% | 房间不能骑在墙上 |
| 附 | `rect_share ≥ 0.30` | 矩形占比下限 |

**`qa_structural.py` — I1–I10 不变量门禁**

**只读，不可改，不可移。** 读存档 JSON，不做任何写入。当前基线：**ERROR = 37**，FAIL 仅 c009（I1 柱族锚定 + I2 柱或板 + UA 降级）。

典型不变量：I1 柱族锚定、I2 柱或板、I8 ……

### 2.9 渲染与审计层

| 脚本 | 产物 | 目录所有权 |
|---|---|---|
| `_dxf_cad_render.py` | **忠实源图纸**（白底黑线） | `dxf_plan/` |
| `_dxf_png_batch.py` | 识别叠加图（灰细线 + 彩色识别结果） | `dxf_plan_recog/` |
| `_dxf_compare_render.py` | A\|B 对比页 | `dxf_plan/index.html` |
| `_dxf_audit.py` | 覆盖度审计（带洞覆盖 + 门 mask） | 只读 |
| `_dxf_audit_report.py` | 审计报告 | 报告文件 |

> **产物目录归属铁律**（2026-09-10 事故）：
> 一个产物目录**只能有一个所有者脚本**。`_dxf_cad_render` 与 `_dxf_png_batch` 曾**都写 `dxf_plan/`**，
> 于是 `python _dxf_png_batch.py c006` 一次干了两件坏事：把 48 栋总目录覆盖成 1 条，把 c006 的**忠实源图纸**顶成叠加图。
> 已修：`_dxf_png_batch` 输出改到 `dxf_plan_recog/`；`_dxf_cad_render.py:418` 本就有单栋保护。
> 事后核对方法：扫全部楼 `dxf_plan/index.html` 是不是同一模板（48/48 全是 A|B 才算干净）。

### 2.10 展示 / 服务层

| 组件 | 位置 | 端口 |
|---|---|---|
| 建模网页控制台 | `control.py` + `run_step.py` + `control.html` | 8130 |
| 房间查询服务 | `backend/db/serve_rooms.py` ⚠️ **不在 `backend/web/`** | — |

`backend/web/run_step.py` **真实 import `run_building`**（所以 `run_building.py` 必须留在根目录）。
`build_index.py` 生成 `data/buildings/index.json`，供网页选择器消费。

---

## 3. 阈值总表（速查）

| 类别 | 常量 | 值 | 出处 | 防什么 |
|---|---|---|---|---|
| 弧 | `CURVE_MIN_R` | 2.0 m | classify | 小弧误判 |
| 弧 | `CURVE_STEP_MM` | 250.0 | classify | 离散精度 |
| 板 | `SLAB_RATIO` | 1.0 | classify | 板当墙 |
| 门 | `DOOR_LEAF_MIN/MAX` | 0.6 / 3.0 m | classify | 门扇长度 |
| 门 | `DOOR_SPAN_MAX` | 3.0 m | classify | 门洞跨度 |
| 门 | `DOOR_JAMB_MIN/MAX` | 0.10 / 0.40 m | classify | 门垛 |
| 门 | `DOOR_SYMBOL_SPAN_MAX` | 3.0 m | classify | 符号跨度 |
| 门 | `DOOR_SYMBOL_TOL` | 1e-6 | classify | 端点容差 |
| 门 | `JAMB_LEN/W` | 0.34 / 0.24 m | classify | 门垛矩形 |
| 门 | `_hinge_leaf` 相等容差 | ≤ 5% 相对差 | floor.py | 墙垛/窗当门 |
| 门 | 打穿度量位移 | ± 0.4 m | 度量脚本 | 穿墙判定 |
| 墙 | `wall_min` | 0.08 m | geometry | 细线当墙 |
| 墙 | `wall_max` | 0.35 m | geometry | 粗环当墙 |
| 墙 | 配对重叠 | > 0.3 | geometry | 虚接触 |
| 墙 | `INNER_WALL_CENTROID_FACTOR` | 0.75 | classify | 内墙归属 |
| 楼层 | `FLOOR_CLUSTER_GAP` | 30000 mm | classify | 楼层分带 |
| 门洞 | 盒深 | `max(外,内) + 0.05` | floor.py | 洞中洞 |
| 门洞 | `MIN_PART_AREA` | 0.03 ㎡ | _door_punch_apply | 碎屑 |
| 门洞 | `SLIVER_ABS` | 1.5 m | _door_punch_apply | 细条 |
| 门洞 | `MAX_LOSS_PCT` | 8.0 % | _door_punch_apply | 墙面积损失上限 |
| 门洞 | `BIG_WALL` / `BIG_KEEP` | 5.0 / 0.80 | _door_punch_apply | 大墙保 id |
| 门洞 | `DOOR_SYMBOL_FACE_TOL` | 0.25 m | floor.py | 门符号贴面 |
| 门洞 | `DOOR_CENTER_FACE_MARGIN` | 0.20 m | floor.py | 门心离面 |
| 门禁 | `is_blob_floor` | 覆盖率 > 20% | recognize | 粗环识别 |
| 门禁 | `verify_floor` A | > 8 条墙 | floor.py | 识别失败 |
| 门禁 | `verify_floor` B | ≤ 20% | floor.py | 覆盖率 |
| 门禁 | `verify_floor` C | ≤ 40% | floor.py | 无墙板 |
| 门禁 | `verify_floor` D | ≥ 85% | floor.py | 门贴墙 |
| 门禁 | `verify_floor` E | ≤ 10% | floor.py | 房间骑墙 |
| 门禁 | `rect_share` | ≥ 0.30 | floor.py | 矩形度 |

---

## 4. 全局不变量与语义约定

### 4.1 楼层 JSON Schema

```jsonc
{
  "walls": [
    {
      "id": "…",              // ⚠️ 必须唯一且存在（build_standard_glb:149 强制）
      "type": "outer|inner|parapet",
      "thickness": 0.24,
      "height": 3.6,
      "poly": [[x,y], …],
      "holes": [ … ]
    }
  ],
  "windows": [ { "wallId": "…", "synthetic": true, … } ],
  "doors": [ … ],
  "stairs": [ … ],
  "columns": [ … ],
  "rooms": [ … ],
  "elevators": [ … ]          // ⚠️ 建模层目前不消费
}
```

### 4.2 铁律

1. **一个产物目录只有一个所有者脚本**（§2.9）。
2. **门 = gap 不是 hole**，盒深必须 > 墙厚。
3. **门洞必须在 recognize 和 wall_thin 两处都挖**。
4. **墙 id 唯一**；切墙后最大块继承原 id。
5. **楼层内部从 0 开始，显示从 1 开始**。
6. **FROZEN = {c006, c009, c103, c104}** 不得批量重建。
7. **`qa_structural.py` 只读**，不可改。
8. **不得跑 `run_batch.py`**。
9. **不得对有 `.orig` 备份（曾人工修过楼层）的建筑跑 `run_building.py`**。
10. **`D:\gym3d` 不是 git 仓库** —— 改引擎/数据前手动备份 `.orig` + 数值化复验 + 失败自动回滚。

### 4.3 生产路径辨识（最容易走错）

```
❌ recognize() 直接产出的内墙        → 粗环 blob，覆盖率 >20%，不是交付物
✅ recognize() → _wall_thin_batch/force → 真实厚内墙  → 才是交付链
```

---

## 5. 已知局限与代码/注释不一致

### 5.1 代码与注释不一致（2026-09-10 已处理 3/4）

| # | 位置 | 原注释 | 原代码 | 处理 |
|---|---|---|---|---|
| 1 | `_wall_thin_batch.py:6` E 阈值 | 「房∩墙均 ≤ 3%」 | `ov_mean <= 10` | ✅ 改注释为 ≤10%（代码为准，逻辑未动） |
| 2 | `_wall_thin_batch.py:3` blob 前置 | 「count ≤ 5」 | `len(inn) <= 8` 或 `has_giant` | ✅ 改注释补全判据（代码为准，逻辑未动） |
| 3 | `dxf_plan_recog/` 双写入者 | — | `_dxf_png_batch` 与 `_dxf_compare_render` 都写 | ✅ 加新鲜度判断：后者仅在缺失/过期时重画，稳态不再互写；docstring 声明归属 |
| 4 | `qa_structural.py:16` I8 | 「INFO，非失败」 | 发 `WARN` | ⏸ **未动** —— 该文件为只读门禁，需人工决定改注释还是改代码 |

> 第 3 条与 §2.9 事故同源（多写入者），但**风险等级远低于**当年：两边复用同一个
> `_dxf_png_batch.render_floor_png`，产出内容按构造即一致，不存在"互相顶掉"。
> 现已在稳态下消除重复写入，并把"谁是所有者"写进两个文件的 docstring。

### 5.1b 遗留命名（已文档化，未改行为）

`backend/modeling/build_standard_glb.py` 的 `DATA` / `OUT` 默认值仍沿用理化楼命名
（`data/lihua-building.glb`）。这是**独立运行时的兜底默认**，管线里由
`run_step.py:104-109` 按楼覆盖。已在该模块 docstring 里写明，避免误读为"用途说明"。

### 5.2 功能性缺口

- `elevators[]` 建模层不消费（电梯缺轿厢）。
- 窗全为 `synthetic=True`（窗缺框）。
- 楼梯缺扶手。
- 13 栋建筑 `sw=0`（无窗）。
- c059–065 / c056 / c057 存在约 **1.5 m 楼梯井碎片**。
- ny27–29 人才公寓未处理。
- 约 **32 个 GLB 过期**（数据更新后未重出）。

### 5.3 结构性问题（详见 `三维建模·算法总结与下一阶段规划.md`）

> **管线没有对象模型。**

墙的真相分散在三个互不相识的阶段（classify / floor / wall_thin），门洞盒被一个开关关住，内墙在两处各生成一次，修复只能靠外挂脚本——根目录曾有 **100+ 个 `_*.py` 一次性脚本**，正是同一症状：**修复沉淀成脚本，而不是能力**。

---

## 6. 标准操作序列

```bash
# 0) 一次性：DWG → DXF（audit=1 是铁律）
python -u convert_dwg_to_dxf.py <name>

# 1) 建/更新档案（含 classifier 分派开关）
#    profile.json 已存在则跳过

# 2) 识别 → 楼层 JSON
python -u run_building.py <name>

# 3) 内墙重建（**交付链的真内墙**）+ 门洞打穿
python -u _wall_thin_batch.py <name>          # 批量生产线
# 单栋强制：python -u _wall_thin_force.py <name>

# 4) 门禁（任何改动后必跑）
python -u qa_structural.py

# 5) 出 GLB（不重跑 recognize 时用 _glb_only）
python -u _glb_only.py <name>
# 或全链：python -u run_building.py <name> --glb

# 6) 出图纸
python -u _dxf_cad_render.py <name>           # 忠实源图纸 → dxf_plan/
python -u _dxf_png_batch.py <name>            # 识别叠加图 → dxf_plan_recog/
python -u _dxf_compare_render.py              # A|B 对比页 + 总目录

# 7) 审计
python -u _dxf_audit.py <name>
python -u _dxf_audit_report.py

# 8) 刷新索引
python -u build_index.py
```

**外科式修单栋**（不整栋重跑，避免窗数变化）：
```bash
python -u _door_punch_apply.py <name>     # 备份 → 挖洞 → 数值复验 → 失败自动回滚
```

---

## 7. 脚本索引

### 7.1 根目录保留（19 个，管线/生产/门禁/回归资产）

| 脚本 | 角色 |
|---|---|
| `run_building.py` | 单栋管道入口（`backend/web/run_step.py` 真 import） |
| `run_batch.py` | 批量管道（**铁律：不许跑**） |
| `convert_dwg_to_dxf.py` | DWG→DXF，管线第一步 |
| `build_index.py` | 生成 `data/buildings/index.json` |
| `qa_structural.py` | I1–I10 不变量门禁（**只读**） |
| `_wall_thin_batch.py` | 内墙重建生产线 |
| `_wall_thin_force.py` | 内墙重建单栋强制 |
| `_dxf_cad_render.py` | 忠实源图纸所有者 |
| `_dxf_compare_render.py` | A\|B 对比页 |
| `_dxf_png_batch.py` | 识别叠加图 |
| `_dxf_audit.py` | 覆盖度审计 |
| `_dxf_audit_report.py` | 审计报告 |
| `_sweep_modeling.py` | 全仓缺陷扫描（回归用） |
| `_door_punch_apply.py` | 门洞外科式落地 |
| `_run_c019_tmp.py` | 被 `_door_punch_apply` 报错信息引用 |
| `_probe_c019_thin_dryrun.py` | 干跑验证 |
| `_probe_doorbox_equiv.py` | `_door_boxes` 等价性单测 |
| `_glb_only.py` | 只重出 GLB，不重跑 recognize |
| `_test_curve_pair.py` | 弧配对单测 |

### 7.2 `_scratch/` 归档（4 批共 340 项）

根目录只保留 19 个管线/生产/门禁/回归脚本；其余全部归档（**只移动不删除**）。

| 批次 | 内容 | 项数 |
|---|---|---|
| ① `_MANIFEST.json` | 根目录一次性 `.py` | 107 |
| ② `_MANIFEST_DEBRIS.json` | 根噪音 + blender 一次性诊断 | 50 |
| ③ `_MANIFEST_DEBRIS2.json` | `data/`、`backend/` 碎片与临时目录 | 40 |
| ④ `_MANIFEST_DEBRIS3.json` | `legacy/` 历史遗留整目录与死代码 | 143 |

`legacy/` 收的是理化楼/六教时代遗留：`archive/`、`render_c103/`、`_al_check/`、
旧 `backend/extract`、旧 `backend/modeling` 建模器、`data/` 的理化楼产物、`PLAN.md`、根 `spec.json`。

**它们仍然有用**——出问题时是最快的取证入口——所以只归档不删除。

- 还原：`python _scratch/_unarchive.py --apply`（读全部 4 份清单，**340/340 可逆**）
- 导航与安全闸说明：`_scratch/README.md`
- 归档前三闸：真实 import 检查 / 按名引用检查 / 明确不碰 `.orig`·`_qa/`·活产物

### 7.3 后端

| 路径 | 职责 |
|---|---|
| `backend/recognizer/classify.py` | LWPOLYLINE 分类器 |
| `backend/recognizer/classify_line.py` | LINE+INSERT 分类器（c006） |
| `backend/recognizer/component_library.py` | 识别经验固化库 |
| `backend/recognizer/geometry.py` | 配对与轮廓推导 |
| `backend/recognizer/floor.py` | `extract_floor` 22 步 + `verify_floor` |
| `backend/recognizer/recognize.py` | 13 步识别主链 |
| `backend/recognizer/profile.py` | `BuildingProfile` + 坐标数学 |
| `backend/recognizer/curve_walls.py` | 弧墙配对 |
| `backend/recognizer/detect_params.py` | 参数探测 |
| `backend/recognizer/standard.py` | `STANDARD` 常量 |
| `backend/modeling/build_standard_glb.py` | GLB 建模 |
| `backend/db/serve_rooms.py` | 房间查询服务（⚠️ 不在 `backend/web/`） |
| `backend/web/run_step.py` | 控制台步骤执行器 |
| `control.py` / `control.html` | 建模网页控制台（:8130） |

---

## 8. 文档地图

| 文档 | 内容 | 状态 |
|---|---|---|
| **`算法与流程总览.md`**（本文） | 全链路算法与阈值 | ✅ 2026-09-10 新建 |
| `三维建模·算法总结与下一阶段规划.md` | 诊断 + 三阶段规划 | ✅ |
| `建模方法论·现状·缺口·专业优化路线.md` | 方法论现状与缺口 | ✅ |
| `楼层对齐修复方案.md` | 楼层对齐 | ✅ |
| `楼层错位排查·思路复盘.md` | 方法论：构件对应法 | ✅ |
| `R12·宿舍公寓簇墙解融·复核·2026-09-09.md` | 墙解融复核 | ✅ |
| `kb/` | CAD 识图知识库（00-overview … 08-su-reference） | ✅ |
| `README.md` | 项目入口 | ⚠️ **过时**：称「理化楼为基线雏形、C006 待修」，目录结构已不符 |
| `PLAN.md` | 理化楼时代计划 | ⚠️ **过时**：称「多楼层组装未做」（早已完成） |

---

## 9. 一句话总结

**DXF 进来，被两个分类器切成四类几何，按 22 步组装成楼层 JSON；但真正交付的内墙要由 `_wall_thin_*` 重建一次、门洞要挖两次；出门禁（`verify_floor` A–E + `qa_structural` I1–I10）之后才出 GLB 和图纸。**
全链最大的风险不在算法精度，而在 **归属与阶段职责**：一个目录一个所有者、一处数据一个生成者、一次修复进能力而不是进脚本。
