# 楼层错位排查 · 思路复盘（2026-09-08 → 09-09）

> 用途：把这两轮「楼层错位」排查里**怎么想**的固化下来——尤其是用户的判据，和我几次
> 认知转折。`楼层对齐修复方案.md` 是第 1 波（批量修 22 栋）的中期记录；本文是**完整路线**，
> 含第 1 波之后对"剩 3 栋"的深挖与反转。读这篇就够了，旧文只作档案。

---

## 一、时间线：想法是怎么一步一步变的

### 第 1 步｜观感报障，别急着给结论
用户原话是「很多楼楼层没有对齐」——一个**观感**（GLB 里层层错开、散架、某层鼓出来）。
没有指标，没有楼层号。第一步永远是：先有能反复跑的工具，把"感觉"变成"数据"。

### 第 2 步｜第一轮：用质心量化，批量修 22 栋
- 造 `check_alignment.py`：逐栋算每层 `outline` 质心的 X/Y 最大漂移，>1m 标红。
- 结论一：48 栋里 ~24 栋漂移 >1m。根因集中在 `to_local` 的坐标系假设——
  「uniform 楼同 X 列、均匀 Y 间距」在 DXF 里普遍不成立。
- 修复只改 `profile.json`（`x_range/cx/cy/floor_ys/floor_plans/outline_unify`），不动识别核心：
  - 首层/塔楼分列 → `floor_plans`；非均匀间距 → `floor_ys`；轮廓碎片化 → `outline_unify`。
- 中期结论：22 栋修完，剩 3 栋（c006/c027/c103）「质心差是裙楼+塔楼的形状差，非错位」。

### 第 3 步｜反转：中期结论被用户的追问推翻
对剩余 3 栋的「不用改」结论，用户没直接信，而是要我**复核**（c006/c027/c103 逐个查）。
复核方法升级——不再拿质心/轮廓说事，而是看**真实构件**（柱、楼梯、墙、同房间）。结果：

| 楼 | 第 1 波判定 | 复核结论 | 处理的最终结果 |
|----|------------|----------|----------------|
| c006（逸夫楼） | 塔楼居中、质心误报 | ✅ 确实没错位 | 居中即可，重载确认（不改几何） |
| c027（第三教） | 塔楼嵌套、非错位 | ❌ **真偏了** | 加 `floor_plans` 逐层实测内容中心，回正 |
| c103（第十一教） | 双翼对称退台 | ❌ **真错位** | 三层污染全清（见下） |

**教训：上一轮"质心差 < 判定阈值 → 不算错位"的推断，会漏掉真错位。** 质心只能筛出
"可疑"，不能给出"没问题"的结论。给"没问题"要凭构件。

### 第 4 步｜定型：c103 是把方法论吃透的样板
c103 的观感是「底层南边鼓出来一圈」。质心法看不出（它在阈值内），构件法挖出**三个独立污染**：

1. **F0 南鼓 90.8m** ← DXF「4.2墙体」层把**入口台阶 + 18.9m 宽大平台**画在了底层平面上。
   图纸证据：南墙以南 tread 线间隔 283mm/级（台阶踏面）、平台宽 18.9m、全部以楼体中心
   **镜像对称**（东/西两个入口）。识别器 union 外墙时一起算 → F0 被撑到 -45.9。
   **为什么只有 F0**：入口只在底层，上层轮廓天然干净——干净的邻居恰好把 F0 的脏照出来。
   **修法**：F0 ← 整体克隆 F1（干净参照系）。台阶/apron 不可修（它们真的是图上一笔），
   只能给一个"没有台阶污染"的同构参照层，让 F0 长得和上面 5 层一样。
2. **F5 悬空柱 20 根（伸到 local -46）** ← `floor_of` 按 **y 距最近楼层中心**归类，对
   **层间空白区**不设防。F4/F5 两行楼之间 y≈999k~1005k 的 4 个孤立柱方块（+顶行自身
   南端柱列）被吸进 F5，换算 local 正是 (±20~62, -40~-46)。下方 0-4 层同 (x,y) **无柱无板**
   （脚下是入口区）→ 绝对悬空。**修法**：F5 柱 ← F1 柱落在 F5 outline 内的子集
   （210→53 根），天然带"脚下必须有柱"的跨层约束。
3. **每层房间怪胎**（2800+ 点、y 伸到 -63.5，渲染成浮空板）← 房间多边形没裁进各自轮廓。
   **修法**：每层房间与 outline 求交（MultiPolygon 分片，<0.5㎡ 丢弃）。

**验证**（全绿，见第三节）。

---

## 二、用户的核心想法（贯穿始终的判据）——务必长期遵守

1. **判定错位，看真实构件的上下对应，不看指标。**
   原话（2026-09-09）：「错不错位，你看上下楼的**柱子**或者**楼梯**，或者**墙面**是否对应，
   但也确实有**上层比下层小**的情况，你可选择几个同样的**房间**是否对齐的方式。」
   即：① 同尺寸房间跨层质心对齐；② 柱 (x,y) 线上下贯通；③ 墙身竖直。三条任一不对 → 真错位。
2. **合法退台不算错位。** 上层比下层小（塔楼/退台/双翼）是真实几何，别把质心差当漂移。
3. **不许瞎改、要最小可逆。** 改前先备份（`.orig`）、尽量只动 profile/数据层不动识别核心。
4. **每栋是"个案"。** 同一种"错开"观感，背后成因各异（分列/碎片/台阶污染/退台/双翼），
   必须落到 DXF 图纸实况再定论，拒绝一刀切。
5. **敢推翻自己前面的结论。** "越做越好"= 用更实的判据，推翻更虚的判据。
6. **建筑名字用 C-code 前缀**（c006/c027/c103），不单独用中文译名。
7. 报障是观感 → 先给可重复工具 → 数据 → 图纸实况 → 最小修 → 构件法验证 → 渲染给人看。

---

## 三、可复用方法论（以后任何一栋楼都这么查）

```
观感报障
  └─ 1. 工具量化：check_alignment.py（筛可疑，不判无错）
  └─ 2. 结构对应（判定真伪）：
         a. 房间：跨相邻层按「面积±15% + 质心<1m」配对，量质心漂移（≤0.01m = 对齐）
         b. 柱：每层柱集合去重后比对；上层柱 (x,y) ⊆ 下层柱集合 ⇒ 100% 贯通（0 悬空）
         c. 墙身：GLB 几何直接投影成「高×深」剖面 ASCII，看是否竖直、层带是否收得干净
         （模型看不了图 → 一切验证走数值 + ASCII/PIL 程序化，PNG 渲染是给人看的）
  └─ 3. 图纸实况：开 DXF，把"多余"的元素定位到真实图面（层名/坐标/形状特征：
         台阶 tread≈283mm 间隔、平台大框、对称性、楼层行间空隙的杂元素）
  └─ 4. 最小修（可逆）：首选"克隆干净参照层/复用贯通构件"，不修污染源本身
  └─ 5. 渲染（Blender render_views.py front/side/iso/top）给用户目视确认
```

**几条已被反复验证的经验：**

- 质心指标对「回字/退台/多翼」必误报（形状质心≠几何中心）；几何中心看包围盒中点。
  例：c103 check_alignment 报 dy=2.65 全是形状差（回字环质心≈4.8 vs 顶带实心质心≈7.44），
  两带包围盒中心实际只差 0.29m。
- 建筑物里"同一层不同构件"要分别查：墙污染会撑大 outline，但柱污染会悬空，
  房间污染会成浮空板——三个独立通道，都要验。
- `floor_plans`/`floor_ys` 的 cy 必须是**该层几何中心**，不能是墙质心（中央区墙多少会上下漂 ~3m）。
- 参考层克隆 F0←F1 成立的前提：F0/F1 逐墙面积几乎相等（先证明同构再克隆）。

---

## 四、最终状态（2026-09-09）

- c006：不改几何，确认居中 ✅（用户重载 GLB 看）
- c027：已加 `floor_plans` 逐层回正 ✅
- c103：F0←F1 克隆、F5 柱收 53 根贯通、房间全裁 ✅；GLB 170×26.1×80.5m；
  0-4 层房间配对 ≤0.01m、柱线 100% 贯通、侧视剖面竖直、F5 平滑退台。
  渲染图 `D:\gym3d\render_c103\*.png`。
- 备份保留：`floor0.json.orig` / `floor5.json.colfix.orig` / `_trim_c103_f0.py`（旧裁剪方案已弃）。

---

## 五、遗留 / 可选项

1. **防复发（识别器层）**：给识别器加 profile 保护的「入口区台阶/apron 检测排除」规则
   （默认关，不碰已调试楼）——根治第①类污染，而不是每次都靠克隆。
2. **c103 顶层性质存疑**：F5 是全宽(±84.5)×浅深(±28)横带、房间全挤在楼体中段与两翼。
   它是回字天井屋面？还是门廊层？值得拿原始图纸/照片核对一次，别默认它=普通顶层。
3. 那 20 根门廊柱若确属地面入口区，可考虑在 F0 入口处按真实落位补回（若想 100% 忠实于图）。
4. `楼层对齐修复方案.md` 的"剩 3 栋非错位"一节已被本文推翻（c027/c103 是真错位），
   该文留档即可，别当现行结论引用。

---

## 六、别再踩的坑（浓缩成一句句）

- 质心差小 ≠ 没问题；质心差大 ≠ 真错位。判真伪只能靠构件。
- "上面比下面小"默认先当退台，别当 bug。
- 修污染要"给干净参照"，不要"修剪脏东西"（修剪会碎环、毁窗洞）。
- 柱子永远要问一句：**它脚下（下一层同位置）有没有楼板/柱？**没有就是悬空。
- 一栋楼三种污染并存时，修完必须**三个通道都验**（房间/柱/墙），别只验一个。
- 改任何 floor 前先 `.orig` 备份；"调试好的"代码和已定论结论，改了要说清为什么推翻。
