feat: ⑪ 判据扩到仓内 kb/ 之外的 .md（510 处带基线）＋ 判据指纹

洞有多大：全仓 54 个 .md 里有 **510 处**「ASCII 引号当中文引号」，落在 13 个文件里。
不是上轮报的 132 —— 那是 `_qa` 那一组的命中处，我把它读成了全仓的洞（两个数都真，
回答的不是同一个问题）。

处置是**基线**而不是批量改：510 处散在 13 份历史记录（台账／复盘／方案／README）里，
批量改 = 改历史；而一条「永久全红」的判据会被学会忽略。

新增／改动：
- `kb/gate.py` ⑪ 第四档 `md-repo`（仓内 `kb/` 之外的 .md，带基线）＋ 基线机制
  · 键 = (文件, 整行 sha12)，**不用行号** —— 行号会随编辑漂，而漂了照样指别处也判绿（铁律 31）
  · 基线自带 `ruler_sha12`（尺子指纹）；尺子变了 ⇒ **整档判不了（红）**，不是提示（铁律 24）
  · `blocked` 与「比过、没有新增」**分开写**：blocked 时 `new` 故意留空，数量走 `blocked_hits`
  · 排除清单排除了什么，在输出里**逐目录数出来**（铁律 23(b)）
- `kb/cjk-quote-baseline.json` 新：41 个文件／510 处／13 个文件的既有命中基线
- `kb/gate_cjk_falsifier.py`    新：刑具 —— S1 键去掉「文件」维／S2 拆尺子指纹／S3 清空排除清单
- `kb/cjk_md_scope_probe.py`    新：只读探针，直接 import `gate._cjk_quote_hits`（同一把尺子）
- `kb/evidence-lock.json`       重登记（49 个证据文件）
- `kb/src/manifest.json`、`kb/kb.json`
  · `check_a5` 的锚点从 `backend/checks/__init__.py:53` 改成 `backend/checks/builtin.py:check_a5`
    —— **行号锚点换成符号锚点**，以后再改那个文件就不会漂（铁律 31）

自造的两个断言缺陷（都当场修掉并写进 selftest）：
- **空断言**：尺子那一支的夹具用空基线 ⇒ 把判断改成 `if False:` 之后**照样绿**
- **假红**：盘上那一组**借了真库的状态**（真库基线当时正 blocked ⇒ `new` 恒空）

收口证据：`kb/gate.py` 退出码 0（⑪ 四档全 PASS）；`--selftest` 12 条刑具全部能红；
刑具三例全红 ＋ 字节级还原（sha256 `37fa4d215f07…`）；`--record` 的 14 条指进 gate.py 的
锚点**全是符号锚点**、逐条读过、无一条漂。
