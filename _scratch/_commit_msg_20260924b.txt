feat: 前台呈现页入库 + 全库补齐「楼层快照落后的房间」，并更正上一条提交里的不实之处

## 〇、先更正：上一条提交（98880dd）里有一句是错的
那条写「已跑 backfill_floor_rooms 落进楼层快照」——**没跑**。今天实测 c001
楼层文件里 12 间、台账 69 间；c002 是 10 vs 25；c003 是 15 vs 36，**三栋全落着**。
提交说明里那句是照"我打算这么做"写的，不是照盘上量的 —— 正是本仓反复栽的那类：
**"我跑过"这句话本身不是量具**，能当证据的只有"落地后再量一遍"。本次每条都补量了。

## 一、为什么这件"落着"很要命（不是多几间房的小事）
`floors/floorN.json` 的 `rooms[]` 是**写盘那一刻的快照**，不是视图 —— rooms.json
后补时它不会自己更新（memory: floor-json-rooms-are-snapshot）。后果是**沉默**的：
  · 3D 查看器点不到房间（逐层房间几何来自这份快照）
  · 识别结果图少画房间（_dxf_png_batch.py 第 92 行读的就是它，脚本自己在第 9 行写了依赖）
两边文件都"格式正确、无报错"，所以只能靠**逐层比条数**发现。

## 二、量具：_scratch/_floor_rooms_stale_scan.py（只读，新增）
逐层比 `floors` 条数 vs `rooms.json` 里 floor==F 的条数，只报"落后/多出"，
**不报"少了多少间以外的推断"** —— 条数一致 ≠ 房间抽对了，那两件事不许混。
首扫：**13 栋落后**，缺 1757 间（其中 c054/c055 各 480、c072/c073 各 304）。

## 三、补了什么、没补什么（没补的都写了理由）
补了 10 栋：c001 c002 c003 c054 c055 c072 c073 c009 c017 c044 c022（含前 3 栋）
  c001 69/69 ✓  c002 25/25 ✓  c003 36/36 ✓  c054 480/480 ✓  c055 480/480 ✓
  c009 146/150  c017 254/254 ✓  c044 105/105 ✓  c022 47/47 ✓
  c072/c073 各 222/402（余下 180 间**质心落不到交付轮廓里**，backfill 放不进去，
    另立一项，不是这次能收的）
**故意没动的三栋**（都有既有笔记作依据，不是"漏了"）：
  · c006 —— 本仓明令「别对它跑 backfill」（它的 rooms.json 带一条陈旧汇总行）
  · c103 —— 楼层文件本身是错层的旧快照（F0 装的是 F1 的数据），按层号回填等于把 F1 的
    房间灌进 F0；修它**须用户点头**，不许自动跑
  · c104 —— F4 是 F3 的平移快照，同理
余下 c034/c028 各缺 1~3 间，**dry-run 回填 0 间**：那几间的质心在轮廓外，回填够不着。

## 四、连带重跑：11 栋 75 张识别结果图（0 失败）
图是磁盘文件，房间来自楼层快照 ⇒ 回填后不重跑，图上画的还是旧快照。

## 五、前台两处可见缺陷一并修掉（frontend/site 首次入库）
· **两条提示互相压掉**：`.stage-note` 自己带 `position:absolute; right/top`，
  页面上有**两条**时各按同一组坐标落进同一格 —— 两行汉字糊成一团，看着像渲染坏了。
  改为容器 `.stage-notes` 负责定位、条目只管排版（窄屏那条也同步改）。
  验证按**几何**验，不按"看着还行"：两条的矩形 362–397 / 400–435，无重叠。
· 楼层表把 5/13/2/2/23/24 逐层显示出来（回填前是 0/0/2/2/4/4）。
