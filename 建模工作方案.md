# 建模工作方案（重排）

> 2026-09-11 重排。上一版只盯着"快"，把"对不对"漏了 —— 这份把两件事分开、排清依赖。

## 一、先说清一个要命的顺序问题

我先前建的安全网（49 栋 GLB 逐字节 sha256 基线）保护的是**当前输出**。
而当前输出你说不对。

**所以那个基线现在不能当验收标准** —— 拿它当标准，等于把错误固化下来，
谁修对了谁反而"不通过"。这是上一版方案最大的逻辑漏洞。

正确的顺序：

```
先修对  →  重新存基线  →  再拿基线担保"改快不改样"
```

**但两条轨道可以并行**，因为：

- **轨道 A（修对）** 动的是**识别引擎** —— 决定"有哪些墙"。
- **轨道 B（GPU/数组化）** 动的是 **MeshBuilder 出三角形** —— 决定"已有的墙多快变成模型"。

B 完全不改变 A 的产物内容，只改变速度。所以 B 可以现在就做，
闸门用**现有**那 49 栋基线（B 只要证明自己没改字节即可），
等 A 修完出了新基线，B 再对新基线核一遍。

---

## 二、轨道 A · 修对（主体工作量）

### A0 把"不对"变成可数的清单（只读，先做这个）

你看出来的三类病，现在的 `qa_structural.py` **一个都测不到** ——
它只查"柱是否锚定在轮廓内""有没有楼板"这类**结构不变量**，
所以它报 48/49 PASS，而你看着不对。**它量的不是你对的东西。**

原料是齐的：每栋 `data/buildings/<楼>/dxf_plan/`（源图）+ `dxf_plan_recog/`（识别后）
+ 现成的 `render_floor_pngs.py`（每层 DXF vs 识别墙叠图）。

要补的量，逐栋逐层：

| 你看到的病 | 怎么量成数字 |
|---|---|
| 墙缺 / 穿洞 | DXF 墙线总长 中被识别墙覆盖的百分比 → **漏墙率** |
| 糊成一块 | 识别墙面积 / DXF 墙面积 → **过覆盖比**；巨型 blob（面积>50㎡ 的单块墙）计数 |
| 楼层错位 / 悬空 | 相邻层轮廓质心 + 柱位偏移；上层墙是否落在下层楼板足迹内 → **越界率** |
| 门窗楼梯 | 窗洞数 vs DXF 窗符号数；门数/开启方向；楼梯井位置 vs DXF 楼梯符号 |

产出：`_qa/defect_<楼>.json` + 一张排名表（哪栋最烂排最前）。
**这一步不改任何东西**，只出事实。

### A1 按簇修

你点名的三类：

| 簇 | 已知病灶 |
|---|---|
| 宿舍 / 公寓（c044-046、c083-086、c059-065） | 内墙被融合成巨型块（历史 #12）；真楼梯井被巨块埋住 |
| 教学楼 / 大平层（c055、c062、c072、c073、c080…） | 待 A0 出数后再定 |
| c009 / c104 | c009 是 QA 唯一 FAIL（37 ERROR）；c104 标着"欠识别" |

每栋：定位 → 改识别引擎 → 只重识别这一簇 → 重出 GLB → 用 A0 的量复核。

### A2 重存基线

全部修完，重跑 49 栋逐字节基线 —— 这次的基线才是"对的模型"。

---

## 三、轨道 B · GPU + 数组化（与 A 并行）

已在 `_scratch/` 里**证明过逐位等价**，不是纸上推导：

- 判定式数组化：4 栋 × 349,962 盖面 + 187,587 侧壁，逐面与标量 `_face_ok` 全等
- 批处理发射器：4 栋 × 5,472,652 顶点 / 2,204,228 面，**逐位、逐序**与现有 `MeshBuilder` 全等

| 步 | 做什么 | 闸门 |
|---|---|---|
| B1 | `MeshBuilder` 存储数组化（`verts`/`faces` 改 numpy + `nv`/`nf` 计数） | 49 栋逐字节 |
| B2 | `add_region` 延迟批处理 + `_flush()`；即时图元入口先 flush | 49 栋逐字节 |
| B3 | cupy 后端 + **强制 numpy 回退**，`GYM3D_GPU` 开关 | CPU/GPU 两条路径各自 49 栋逐字节 |
| B4 | 进程并行（49 栋互不依赖，本机 20 逻辑核） | 逐字节；**且**与串行结果一致 |

只有 B3 是"用 GPU"。B1/B2/B4 是纯 CPU —— 而且实测**收益比 B3 大**，
因为 GPU 只在顶点数回到百万量级的 `_flush()` 内部才有意义。

### 诚实预期（不承诺）

c006 实测：`add_region` 5.1 s / 总 7.7 s（66%）；c072 实测 16.3 s / 25.1 s（65%）。
即**顶点发射占单栋 2/3 的时间**。B1+B2 打的就是这 2/3。
GPU 叠在上面再省一点，**不会是 100×** —— 那部分是 Python 解释开销，
变成数组运算后本来就只剩 1–2 秒，没有 100 倍可赚。

真正的大头在 B4：27.3 分钟 → 预计 4–5 分钟。

---

## 四、明确不做 / 铁律

- 不跑 `run_batch.py`；不对有 `.orig` 的楼跑 `run_building.py`
- 不动 `data/buildings/*/.orig/`（手工修楼层的唯一来源）；同步脚本不用 `--delete`
- 不改 `qa_structural.py`（只读门禁）
- 冻结四栋 c006 / c009 / c103 / c104 —— **c009、c104 你点名要修，动手前我单独跟你确认解冻**
- 秘密只在 `.env`；提交信息中文，格式 `类型: 说明`；署名已全局禁用

---

## 五、现在开始做什么

先做 **A0**。理由：你没说清"很多"到底是哪些、烂到什么程度，我也没量过。
在拿到这张排名表之前动识别引擎，就是凭猜改代码 —— 我这一轮已经猜错过两次
（"add_box 是热点"、"c084 是平方级"），不想再猜第三次。
