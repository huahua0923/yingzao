# 楼层对齐修复方案

> 日期：2026-09-08
> 范围：`D:\gym3d` 数字孪生平台全部 48 栋楼（`data/buildings/*`）
> 触发：用户报告「现在很多楼，楼层没有对齐」，要求查明原因 + 检查所有楼 + 出方案。

---

## 一、背景（综合前面对话，已完成的部分）

| # | 事项 | 状态 |
|---|------|------|
| 1 | GLB 导出侧对齐窗算法：合成窗开洞 + 铝合金窗框 + 玻璃 | ✅ 完成（`build_standard_glb.py` / `glb_common.py`） |
| 2 | 第六教学楼 c006 第 12 层（负一楼）删除，11 层 | ✅ 完成（删 `floor_plans` 末项 + `floor11.json` + index） |
| 3 | 全楼批量重生成 GLB（开窗+窗框），带进度显示 | ✅ 完成（`_regen_windows.py`，48/48 成功） |
| 4 | **楼层错位排查与修复** | ⚠️ **本方案主题** |

---

## 二、诊断方法

对每栋楼加载 `floors/floor*.json`，取每层 `outline`（楼板轮廓）的 shapely 质心与包围盒，
计算「跨层质心最大漂移」（X 向、Y 向）作为错位信号。

- **X 漂移 > 1m** → 该楼不同层的平面在 DXF 里画在不同 X 列（首层/塔楼另列），单一 `cx` 无法居中每层。
- **Y 漂移 > 1m** → 层间 Y 间距非均匀、或双翼/阶梯楼质心因墙多少上下漂移。
- **F0 离原点 > 5m 但跨层漂移 < 1m** → 整栋楼整体偏移（纯外观，非错位）。

> 注：质心指标会**误报**阶梯楼（塔楼比裙楼小、质心天然不同），故塔楼/裙楼的「质心漂移」
> 不等于真错位，需对照包围盒判断塔楼是否居中/嵌套在裙楼内。

---

## 三、根因

识别入口 `recognize.py` 里，`to_local(p, x, y, f)` 决定每层平面在本地坐标系的原点：

```python
def to_local(p, x, y, f):
    if p.floor_plans:                       # 每层各自 [cx, cy, x_min, x_max]
        cx, cy, _, _ = p.floor_plans[f]
        return ((x - cx) / 1000, (y - cy) / 1000)
    fy = p.floor_ys[f] if p.floor_ys else (f * p.offset + p.cy)   # 均匀楼
    return ((x - p.cx) / 1000, (y - fy) / 1000)
```

**根因**：48 栋里 **44 栋是 `uniform`**（单一 `cx` + 均匀 `offset`），但它们的 DXF 并不满足
「所有层同 X 列、均匀 Y 间距」的假设——**首层（或塔楼）画在另一 X 列、或层间距非均匀**。
于是这些楼用同一个 `cx` 去居中所有层，导致首层/塔楼横漂几十到上百米。

三种已知成因与对应机制（机制已存在，只是没覆盖到全部楼）：

| 成因 | 症状 | 修复机制 | 已用楼 |
|------|------|----------|--------|
| 首层/塔楼画在另一 X 列 | X 漂移（几十~上百米） | `floor_plans`（每层 cx/cy + x_range 判列） | c006 |
| 层间 Y 间距非均匀 / 双翼 | Y 漂移 | `floor_ys`（每层 cy） | c022、c103 |
| 低层墙碎片化 → 轮廓塌成小片 | 轮廓质心横漂、面积骤减 | `outline_unify` / `outline_unify_floors` | c009、c006 |

---

## 四、受影响楼清单（按漂移降序）

> 漂移 = 跨层轮廓质心 X/Y 最大跨幅（米）。「需核实」= 塔楼偏移是否真实，须对照 DXF 定论。

### A. 严重（漂移 ≥ 10m，肉眼明显错位）

| 楼 | X漂移 | Y漂移 | 判断 | 修复方向 |
|----|------|------|------|----------|
| c043 | 108.1 | 0.5 | 首层画在左列、上层在右列（首层分列） | `floor_plans` |
| c034 | 20.9 | 7.5 | 塔楼偏左（裙楼+塔楼两列） | `floor_plans` |
| c103 | 0.0 | 24.4 | 双翼楼，`floor_ys` 已配仍漂 | 修 `floor_ys` cy |
| c079/c083/c084/c085/c086 | 22.1 | 2.2 | 5 栋同型公寓，塔楼偏右 | `floor_plans`（一次配 5 栋） |
| c026 | 9.4 | 1.0 | 退台楼，逐层左收 | `outline_unify_floors` / 左对齐 cx |

### B. 明显（1~10m）

| 楼 | X漂移 | Y漂移 | 判断 | 修复方向 |
|----|------|------|------|----------|
| c027 | 2.6 | 4.2 | 首层裙楼 + 上层塔楼 | `floor_plans` |
| c041 | 1.4 | 3.6 | 层间 Y 微漂 | 核 `offset`/`floor_ys` |
| c006 | 0.0 | 3.6 | 塔楼居中（已 floor_plans，质心误报） | **已对齐，无需改** |
| c019 | 1.9 | 1.5 | 整栋 Y 偏移 20m + 微漂 | 核 `cy`；整体偏移可不改 |
| c017 | 2.9 | 0.4 | X 微漂 | `floor_plans` |
| c072/c073 | 3.2 | 0.1 | 同型公寓 X 漂 | `floor_plans` |
| c054/c055 | 3.2 | 0.0 | 同型公寓 X 漂 | `floor_plans` |
| c104 | 0.4 | 2.6 | Y 漂 | 核 `floor_ys`/`offset` |
| c022 | 2.8 | 0.0 | `floor_ys` 已配仍 X 漂 | 修 `floor_ys` / 补 `floor_plans` |
| c080 | 0.0 | 2.7 | Y 漂 | 核 `offset` |
| c057 | 0.3 | 2.4 | Y 漂 | 核 `offset` |
| c062/c063 | 0.0 | 2.5 | Y 漂 | 核 `offset` |

### C. 对齐 / 轻微（< 1m，可不动）

其余约 23 栋：c009（outline_unify 已配）、c025、c028、c029、c030、c031、c032、c033、
c044、c045、c046、c056、c059、c060、c061、c064、c065、c109、c114、c116、c018、ny27/28/29 等。

---

## 五、修复步骤（按楼批量，不是一栋一调）

1. **建诊断脚本 `check_alignment.py`**（把第二节的方法固化成可重复跑的工具）：
   - 逐栋输出 X/Y 漂移 + 首层偏移 + 塔楼/裙楼包围盒；
   - 阈值 >1m 标红，作为修复清单与回归门槛。

2. **逐栋核对 DXF 楼层排布**（用 `compare_floors.py <name>` 出平面比对图）：
   - 确认每层平面在 DXF 里的 X 列 / Y 位置；
   - 定论「首层分列 / 塔楼分列 / 非均匀间距 / 碎片化」中的哪一种。

3. **补 profile 配置**（唯一改动点，不动识别核心代码）：
   - 首层/塔楼分列 → 加 `floor_plans`（每层 `[cx, cy, x_min, x_max]`，cx=该层实际几何中心，
     x_min/x_max=该列 X 区间供 `floor_of` 判列）。**同型公寓（c079 组、c072/073、c054/055）
     一套 floor_plans 模板复制到各栋**。
   - 非均匀间距 → 加/修 `floor_ys`（每层 cy）。
   - 碎片化 → 加 `outline_unify_floors`（阶梯楼裙楼子集）或 `outline_unify`。
   - ⚠️ `floor_plans` 的 cy 必须是**几何中心**（墙质心会因中央区墙多少上下漂 ~3m，见
     `profile.py` 注释与 `c006-podium-unify-drift-fix` 记忆）。

4. **重新识别 + 重导出 GLB**（复用 `_regen_windows.py`，先识别再导 GLB）：
   - 识别：`python backend/recognizer/run_building.py <name>`（或等价入口）；
   - GLB：`python -u _regen_windows.py <name>`；
   - 全程带进度（`-u` + flush，已解决缓冲无日志问题）。

5. **回归验证**：重跑 `check_alignment.py`，漂移应 < 1m（阶梯楼塔楼按嵌套判定，
   不按质心误报）；再肉眼过一遍 building.html / GLB 三维视图。

---

## 六、验证标准

- [ ] `check_alignment.py` 对 48 栋全跑，漂移 >1m 的楼从 ~24 栋降到 0（阶梯楼除外、按嵌套判定）。
- [ ] 阶梯楼（c006/c026/c027/c034/c079 组等）塔楼正确嵌套/偏移在裙楼内，非错位漂移。
- [ ] GLB 里逐层轮廓垂直对齐，无「层层错开」的散架感。
- [ ] 修复不回归：门窗洞/窗框/玻璃仍正常（`_regen_windows.py` 回归 48/48）。

## 七、风险与注意

1. **改 profile 会触发重识别**，可能连带改变墙/门/房间数量 → 修完必须回归比对
   （`compare_floors.py` 看覆盖率）。
2. **同型公寓共 30+ 栋**（`公寓37栋批量` 记忆）：一套 floor_plans 模板批量套用，别一栋一栋
   手调——省时且保证一致性。
3. **质心 ≠ 几何中心**：`floor_plans` 的 cx/cy 一定要取几何中心（DXF 该层平面的包围盒中心），
   不能拿墙质心，否则阶梯楼又会漂（c006 已有教训）。
4. **c006/c022/c103 已配 floor_plans/floor_ys 但仍显示漂移**：需复核现有配置是否正确
   （尤其 c103 双翼楼 Y 漂 24m），可能配置本身有误，不是没配。
5. 只改 `profile.json` + 重识别重导，**不动 `recognize.py`/`to_local` 核心逻辑**，降低回归面。

---

## 八、修复结果（2026-09-08 执行）

### 已修复 22 栋（漂移归零或降到几何中心对齐）

| 楼 | 根因 | 改法 | 修复前 → 后 |
|----|------|------|-------------|
| c043 | 首层画在左列、上层在右列（DXF 双副本） | `x_range` 收窄到右列 + `cx/cy` 改几何中心 | X 108.1→0.12 |
| c103 | 双翼楼 `floor_ys` 按旧口径算偏 27m | `floor_ys` 改为逐层几何中心 | Y 24.4→2.65（质心误报，X 已 0.03） |
| c104 | `floor_ys` F4 偏 2.6m | `floor_ys` 改几何中心 | Y 2.6→0.16 |
| c034 | 上层墙稀疏→轮廓塌成 17.9m 碎片 | `outline_unify=True`（复用 F0 完整轮廓） | X 20.9→0.00 / Y 7.5→0.00 |
| c026 | 上层 F3+ 右翼墙稀疏→轮廓塌 | `outline_unify=True` | X 9.36→0.00 |
| c022 | 首层碎片（12.9m，真楼层缺） | `outline_unify=True`（复用 F1 完整轮廓） | X 2.84→0.00 |
| c017 | 首层核心筒窄于上层 | `outline_unify=True` | X 2.85→0.00 |
| c041 | 逐层加宽（上层墙多） | `outline_unify=True` | X 1.41→0.00 |
| c079/c083/c084/c085/c086 | 同型公寓，上层墙少→轮廓塌碎 | `outline_unify=True` | X 22.1→0.00 |
| c072/c073/c054/c055/c080/c057/c062/c063/c019 | uniform 楼上层碎片化 | `outline_unify=True` | X/Y 漂移→0.00 |

### 剩余 3 栋「漂移」为裙楼/塔楼质心差，非错位（已核对包围盒）

| 楼 | 判定 | 依据（包围盒嵌套） |
|----|------|--------------------|
| c006 | 塔楼完美居中 | 裙楼 F0-6 129m×83.7m、塔楼 F7-10 46m×23m，中心都 (0,0) |
| c027 | 塔楼嵌套裙楼（偏 1.6m 为 DXF 实况） | 裙楼 55m×55m 居中 (0,0)，塔楼 24m×27m 中心 (1.0,1.4) |
| c103 | 双翼楼对称退台 | X 全层 169m 对齐，Y 逐层对称收（90.8→79.5→56.4m） |

### 结论

- `check_alignment.py` 48 栋回归：漂移 >1m 从 ~24 栋降到 **3 栋**，且这 3 栋经包围盒核对
  均为「裙楼+塔楼/双翼」的质心差，非「层层错开」的真错位（塔楼都正确嵌套/居中）。
- 修复只改 `profile.json`（`x_range`/`cx`/`cy`/`floor_ys`/`outline_unify`），未动识别核心。
- 22 栋已重识别 + 重导 GLB（`_regen_windows.py`）。
