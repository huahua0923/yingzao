# R12 宿舍公寓簇·内墙解融 — 复核报告（2026-09-09）

> 收尾用户连报：**「很多楼墙体太厚 / 整层实心（芙蓉园3）、楼梯无法走入、块体重叠」**。
> 楼梯井误判已在 R11（#18）清除 5 栋；本报告覆盖 R12 墙融合解构（#19）与全量复核（#16）。

## 一、根因
宿舍/公寓单图层图纸的内墙被整体 **union 成一个实心网络**（房间=洞里），而非逐条真墙：
- 整层内墙净覆盖一度 **60~100%**（芙蓉园3 c031 = 139㎡/层 = 29%），前端渲成实心块。
- 门/房/墙三者互差 1~3m（门贴真墙线、房贴融合墙洞）→ 门看不清、房间点不出（见 R11 门普查）。

## 二、方法（每层独立 + 自动回滚）
1. 弃「单线 buffer」原型（双皮线全 buffer → 122㎡ 单网络，FAIL 已回滚）。正确解：图纸墙是**双线皮**。
2. `geometry._flatten_wall_segments` + `geometry.pair_wall_faces`（C006 已验证）：平行皮配对读**真实厚度 0.1~0.3m** 的独立薄墙；未配对段 = 0.1m 单线薄墙。
3. 只替换 `inner` 墙；外墙薄环/女儿墙保留；新墙 clip 到外墙内空间防双覆盖。
4. 触发判据：内墙 union 覆盖 >20% 且（条数 ≤8 或 任单墙 >35% 楼板）——捕获纯 blob（c031）与「巨墙+几小墙」（c026/c018 地面层条数 6~7 首轮漏网）两类。
5. 验收门（任一不过 → 该层回滚，备份保留）：条数>8、净覆盖≤20%、无单墙>40%楼板、**门贴墙≤1.0m ≥85%**、房∩墙均值≤10%、**双线配对面积占比≥30%**（防单线图刷出浮条）。

## 三、覆盖与结果
**已解 16 栋**（用户点名芙蓉园3 + 15 栋同构，含 c026/c018 地面层；备份在各 `data/buildings/<楼>/.orig/floors.before_wallthin/`）：
c018 c026 c027(8层全) c031(6层) c034(6层) c072 c073 c079 c080 c083 c084 c085 c086 c109 c114 c116。
全部楼层数值验收通过并写入；曾回滚案例：c031 首版、c034 F1-3（门贴墙 79~83%）。

**c034 门贴墙探针**：0 真浮门——失败门全部 <1.0m，为 1.5~2m 双开门洞内中点；门阈值 0.5→1.0m（覆盖宽门洞），仅 c034 触发过该阈值，放宽有据。

## 四、全量复核（只读，48 栋）
| 指标 | 修复前（R11末） | 修复后（R12） |
|---|---|---|
| 非冻结残留 blob 楼层 | ~70 层 | **0** |
| 非冻结残留巨墙(>40%楼板)楼层 | ~70 层 | **0** |
| 非冻结最大内墙覆盖 | 74.9%（c026 F0）等 | **≤15.5%**（ny27-29 退台风格，非 blob） |
| 代表楼 | c026 F0 74.9→9.2% · c034 69.5→7.2% · c018 F0 44.7→14.8% | |

**结构不变量门禁 `qa_structural.py`（I1-I10，只读）**：ERROR=37、FAIL 楼=1/48（仅冻结 c009）——
与 6 轮校准基线完全一致，墙重建 **0 回归**；16 栋全部 PASS（WARN 属 INFO 级，柱/板类，非墙）。

## 五、冻结楼（用户「调试好的代码冻结」，仅报不修）
- c104 F0-4、c103 F5 仍为 blob（内墙覆盖 75.9% / 54.9%）；c009 高层 82.5%（多墙重叠，无单巨墙）。已存在相应建模限制记录，留给用户决定。

## 六、产物
- 逐层比对 PNG 已重渲染：`data/buildings/<16栋>/plans/floor{F}.png`（灰=DXF墙 / 橙=门 / 红=轮廓）。
- 渲染日志 `_render_r12.log`；脚本 `_wall_thin_batch.py`（可复跑，幂等）、`_wall_verify.py`、`_c034_doorprobe.py`、`_giant_inspect.py`。

## 下一步（未做，待用户拍板）
- 冻结 c104/c103 若要修，需解冻并单独设计多翼/斜墙分支。
- 「真窗口定位 + 高度可改」功能（从 DXF 读窗，不合成）——R11 遗留，仍未开始。
