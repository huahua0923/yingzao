# 建模方法论：现状 · 缺口 · 专业优化路线

> 日期：2026-09-09。对象：`D:\gym3d` 数字孪生平台「DXF 平面图 → 3D 建筑」。
> 范围：整个识别/建模管线（48 栋 + 理化楼基线），不是某一栋。
> 目的：把"现在怎么建"讲清 → 用专业（BIM/国标制图）办法找"缺什么、怎么补"，
> 重点覆盖 天井 / 阳台 / 退台 / 屋面机房 / 入口雨棚 等疑难。

---

## 一、现状思路（一句话 + 流程）

**"平面图 → 参数化挤出"**：每层从该层 DXF 切片里抽 墙/柱/门/电梯/楼梯/房间，
以"墙中心线 union 外扩"定轮廓 → 存成对象化 `floor{N}.json` → 建模器按
`z = F × layer_height` 堆叠、挤出楼板/墙、给墙挖窗洞、最后罩一个屋顶。

```
DWG/平面DXF ──pipeline.py──▶ 每层识别(classify: 墙皮配对/柱/门/窗洞/房间标签)
      │                         → floor{N}.json(本地坐标, 对象化)  + spec.json(风格常量)
      ▼
build_standard_glb.py:  slab=outline(挖电梯/楼梯井洞) + 三段墙(贴sill/窗台) + 窗notch挖洞+玻璃+铝框
                          + 柱盒 + 门板+门头梁 + 室内楼梯(直/双跑/双分) + 平顶或坡顶(gable) + 女儿墙
      ▼
   GLB  ─▶ frontend/building.html 查看 / control.html 参数面板 / render_floor_pngs 对比
```

能力上的长处（已经被 48 栋验证）：
- **对象化 floor JSON**（每构件带 id/类型/坐标），不是三角面堆 → 可改可验、可回放；
- **图层+国标线宽先于几何**（`arch-rules-over-geometry`）、构件库沉淀识别经验；
- **疑难楼用 profile 开关逐个兜**（退台/两列/碎片化/过渡层/统一 footprint），不动核心代码；
- **多档校验工具**（check_alignment / compare_floors / render_floor_pngs / ASCII 剖面）。

---

## 二、缺什么（按"会炸在哪类楼"分级）

> 严重度：🔴=会直接建错/观感错，🟡=细节缺失，🟢=锦上添花。
> 引用的教训都来自已修楼，不是猜测。

### A. 几何构件能力缺口（识别器把"对象"当"墙"吃掉的家族根源）

| # | 缺口 | 现象 / 触发楼 | 严重度 |
|---|------|--------------|--------|
| 1 | **入口台阶/平台/雨棚**没有被当对象 | c103 F0 南鼓 90.8m：台阶/apron 在墙体层被 union 进轮廓。靠"整体克隆 F1"绕开，根没除 | 🔴 |
| 2 | **天井/回字中庭**只被动保洞，无"内立面"概念 | c103 是 229㎡ 环带+6506㎡ 天井洞；内圈墙上的窗/房间朝向、F0 天井底（无板）靠运气 | 🔴 |
| 3 | **阳台（凸/凹/封闭阳台、挑廊）** 0 支持 | 全仓 0 命中。外立面上的薄条带会被当墙或当污染 | 🔴 |
| 3b | **弧面/曲墙/异形墙**（礼堂半圆后墙、门厅弧墙、切角斜墙）只对斜墙"恰好能建" | 全仓真弧墙 0 命中（2026-09-09 扫描确认）。若出现会被当**折线段墙**：墙皮配对碎、合成窗按段布、外轮廓呈多边形锯口；`wall.union` 沿弧每段外扩还会在顶点留缝 | 🔴（一旦出现必错） |
| 4 | **屋面设备（电梯机房/水箱/楼梯间出屋面）** 显式丢弃 | 识别里 >100m 副块被过滤；c006 老脚本有"第12层设备层"概念但标准管线不建 | 🟡 |
| 5 | **退台（上楼比下楼小）** 能排布不能断言 | 只靠 floor_plans/transition/wingRoof 摆对位置；缺"顶层收进合法"的自动判定，全靠人核 | 🟡 |
| 6 | **连廊/天桥/风雨廊**（教学楼两翼间上层的连廊）0 | 会被当"悬空墙碎片" | 🟡 |
| 7 | **玻璃幕墙 / 通长带窗** 0 | 只有"沿直墙等距布合成窗"；幕墙会变成一圈小窗 | 🟡 |
| 8 | **地下室/负层、阁楼层、坡屋顶下阁楼、错层/夹层** 0 | `z=F*floor_h` 假设非负等距；首层高常>标准层但全局固定 4.2 | 🟡 |

### B. 数据 / 语义缺口（只用了平面图这一个信息源）

| # | 缺口 | 说明 | 严重度 |
|---|------|------|--------|
| 9 | **立面/剖面图、门窗表、楼层表 未读取** | 平面图没有：真实窗宽高与排布、层高逐层值、坡屋顶坡度、外墙材料、女儿墙高。现在窗=合成布点（`facade_windows` 等距生成，标 `synthetic`），屋顶坡度=猜（rise=min*0.42），层高=写死 4.2 | 🔴（准度上限） |
| 10 | **轴网没提取**，柱不吸网格 | 有 4.1结构柱图层但没建"轴网→柱位"约束；柱漂移/悬空/多副本只能事后查（c103 F5 伪柱、公寓 37 栋多副本） | 🟡 |
| 11 | **房间语义只用于导航/染色，未反哺校验** | rooms 已带名称（MTEXT 标签），可用来断言"楼板面积≈房间+交通+天井"，抓漏建 | 🟢 |

### C. 一致性 / QA 缺口（错位排查是一次性人工，不是门槛）

| # | 缺口 | 说明 | 严重度 |
|---|------|------|--------|
| 12 | **无"结构体检"自动门槛** | 柱线连续、外墙垂向对齐、每层开口在墙上、天井跨层一致——这些"构件对应法"现在靠人报再查。应变成每栋生成后的强制回归 | 🔴 |
| 13 | **静默丢弃不报错**（屋顶副块、>100m 离群柱/门被滤掉不留痕） | 建模器不知道自己"丢过东西"；观感对不上时无误差面可查 | 🟡 |
| 14 | **渲染正确性验证**只能靠人眼（模型看不了图） | 已用 ASCII/数值补，但没有统一"每栋自动出体检报告" | 🟢 |

---

## 三、专业办法怎么补（BIM-lite 思路）

核心是把识别从"**图上有线 → 猜构件**"升级为"**先有构件语义，再让线去填空/验真**"，
并借用三个国标/BIM 常规做法：

### 1. 分层建模，别让通用墙识别吃"特殊对象"

```
第 0 层  图层/图框语义：楼层表(几层/首层高/有无地下室) ← 图框标题栏
第 1 层  结构定位：轴网 + 柱 → 柱线必须落在网格交点上(吸网格)
第 2 层  主体构件：外墙/内墙(含厚度) + 楼板(outline)
第 3 层  特殊对象(专设识别分支, 不进通用墙 union)：
           入口台阶/平台/雨棚 → 单独的"室外构造"类型
           阳台/挑廊          → 贴外墙面薄板+栏板, 判定后从轮廓排除
           天井内圈墙+朝向    → 内立面窗/房间
           屋面设备(机房/水箱)→ 顶层附加体块
第 4 层  开口与装饰：门/窗(读门窗表或立面图, 合成仅兜底)/玻璃幕墙
```

**为什么这能根除 c103 那类病**：入口台阶/apron 不再是"要被 union 的墙"，而是"有自己
坐标、深度范围、属于 F0 但不属于 F0 轮廓"的室外构造对象 → F0 轮廓天生干净，
不需要"克隆 F1"这种绕法。

### 2. 加第二信息源：立面/剖面图 + 门窗表（如果 DWG 里有）

平面图给不出：真实窗宽高/窗台高/窗排布、逐层层高、坡屋面坡度、女儿墙高、外墙材料。
**专业流程是平面定位置、立面定高度、剖面定剖面、门窗表定门窗尺寸**。建议：
- 侦查阶段检测 DWG 是否含 `门窗表`/`立面`/`剖面` 块；
- 有 → 建"门窗库"（型号→宽高）回填开口，立面图给逐层层高表；
- 没有 → 维持合成窗 + 规则窗墙比，但**显式标注 synthetic**，别让它冒充真实窗。

### 3. 建模不变量 = 每个模型的门禁（把"构件对应法"固化）

生成后自动跑，任何一栋都必须过，过不了就是 bug（而不是"待人工核实"）：
1. **柱线连续**：除首层外的每根柱，脚下同 (x,y) 必须有柱或明确的基础板（退台处除外并标注）；
2. **外墙垂向对齐**：相邻层外墙中心线在墙厚容差内重合；不重合处必须是已登记的退台层；
3. **楼板守恒**：每层楼板面积 ≈ Σ房间 + 交通(梯/电梯/走廊) + 天井开洞；缺一块就是漏建；
4. **开口在墙上**：每扇门/窗的落点墙厚 > 0；
5. **无静默丢弃**：凡识别/建模过滤掉的离群构件，记入 `qa_notes`，不悄悄消失；
6. **天井跨层一致**：被判为天井的开洞在各层 x/y 一致（允许某层桥接=连廊，另记）；
7. **异形/弧面墙登记**：非正交斜墙登记（c103/c104 类合法）；连续同向小转角的 8+ 段墙 = 疑似弧面，
   必须整体按弧/曲面建模、窗沿弦或弧均布，不得按折线段墙处理。

> **落地工具：`D:\gym3d\qa_structural.py`（2026-09-09 交付，阶段 2）。**
> 只读 `floors/floor*.json`，把上面不变量细化为 I1–I10 逐栋出体检报告（`_qa/<楼>_qa.txt`）：
> I1 柱在自身轮廓内(柱族锚定判据) / I2 柱上下贯通(柱或板判据+楼级抖动聚合) / I3 房间在轮廓内 /
> I4 墙不外溢 / I5 外扩带空腔 / I6 天井登记 / I7 重复柱(聚合楼级) / I8 体量堆叠 / I9 异形弧面墙 /
> I10 板外柱-墙内柱(c009 类楼板过小)。完整语义以各函数 docstring 为准（经过 6 轮校准，见阶段 2 末）。
> 用法 `python qa_structural.py [楼...|--all-verbose]`，汇总表按 ERROR/WARN 判 FAIL/CAUTION/PASS；
> 欠识别楼（`profile.under_recognized` 且确有 ERROR）标 UNDER 不计 FAIL（ERROR 降级为带"(欠识别待重建)"前缀的 WARN）。
> 注意：这是**读存档 floor JSON 的门禁**，不是重识别；不可与 `run_batch.py`（会重写楼层）串进同一写流程。

### 4. 疑难形态的"专业处置卡"（决策树）

| 形态 | 判别依据（图纸可测） | 处置 |
|------|---------------------|------|
| **天井/回字** | outline 有 ≥1 内孔；孔面积大、孔内无墙无柱；该孔跨层存在 | 孔=天井。F0 该孔不铺板（院子地面）；内圈墙算"内立面"，窗朝孔开；孔在某层被桥接=连廊 |
| **阳台** | 外墙外侧有 1.0~1.8m 深、与墙平行、含开口门的薄条带；凸出轮廓 | 挑板+栏板(h≈1.05)对象；从轮廓/外墙判定排除，不污染 union |
| **退台（上楼小）** | 上层轮廓 ⊂ 下层轮廓（几何真包含）而非 X/Y 平移 | 合法；垂直柱线在收进处终止并登记，别按"错位"查 |
| **屋顶设备** | 顶轮廓内靠塔楼/楼梯间上方的密集小块（有柱/有墙围合） | 附加体块：机房盒+女儿墙+检修口；柱脚在顶板上非悬空 |
| **入口雨棚/门廊** | F0 外墙外、紧贴门、悬挑形状；与台阶同源 | "室外构造"对象：板标高=F1 板，不从 F0 轮廓出；彻底告别"克隆干净层"兜底 |
| **幕墙/通长窗** | 某立面直墙很长且窗连续；或图上有幕墙分隔线 | 识别为幕墙类型：整段玻璃+竖向龙骨，不逐个布小窗 |
| **弧面/曲墙** | 墙 poly 顶点≥8 且转角序列同号 0.5~15°、弓高/弦长>4%（qa I9 已能探） | 按单对象建弧墙：保留圆心/半径+起止角，union 沿整弧一次外扩（勿逐段），窗沿弧等角均布；开口 notch 用弧段投影 |
| **异形/切角斜墙** | 墙段方位角偏离正交 >5°（c103/c104 已命中） | 合法，按折线段建模即可；qa I9 只登记（INFO），窗仍沿直段布 |
| **首层高≠标准层** | 图框/剖面显示首层 4.5~5.4、标准层 3.6~3.9 | `floor_h` 逐层可覆盖（现在全局写死 4.2，需改成每层 layer_height 生效） |
| **地下室/负层** | 图框标注 -1F；或首层平面图上另有深埋墙轮廓 | 支持 z<0 堆叠 + 侧壁土体边界（可选）；至少先把 -1F 平面建出来 |

---

## 四、落地路线（建议分阶段，每阶段一个可验收门槛）

> 顺序按"投入产出 + 消灭最痛的观感病"。每条都尽量复用现有 profile 开关模式（默认关、不破坏 48 栋基线）。

- **阶段 1 · 让模型知道自己丢了什么（最便宜、立刻止血）**
  把"过滤离群构件 / 屋顶副块 / >100m 柱门"改成**记入 qa_notes 并随 spec 输出**；
  建模器末端加"未渲染清单"。→ 观感对不上时第一件事能查。
- **阶段 2 · 结构体检门槛（把构件对应法自动化）— ✅ 2026-09-09 已交付 + 6 轮校准**
  上节 7 条不变量落地为 `qa_structural.py`（I1–I10），每栋跑出体检报告 `_qa/<楼>_qa.txt`。
  **6 轮校准后 48 栋终态：ERROR=37 WARN=108 FAIL 楼=1/48（仅 c009），CAUTION 14，UNDER 1（c104），PASS 32。**
  校准要点（把首跑的误报/双报全清掉，只留真信号，别被假阳淹了）：
  - **I1 柱族锚定**（R4）：同一 (x,y) 柱族只要 f0 有同轴柱(落地)或踩到某层楼板 = 合法门廊柱/转换/挑出 → **WARN**；
    柱族一路无柱无板直到某层起跳才悬空 = 真·悬空鳍 → **ERROR 只在起跳层每族一条**，上层延续合并 INFO，不逐层逐根刷。
    → c006 那 10 根南面柱是通高门廊柱(c103 同类)从 ERROR 降为 WARN；c009 的 185 条(5 层×37)收敛为 37 条 f1 起跳 ERROR。
  - **I2 柱或板**（R5）：站下层楼板上的柱 = 合法支撑(用户不变量"柱 **或** 板")，**不再报**"无同轴下层"；
    自己楼板内却脚下无下层板的(退台/挑出)才 WARN；自己楼板外悬空鳍归 I1，I2 不双报。
    楼级把"同脚印楼层组内柱数小幅抖动"(公寓 c079: 305/310/330/298)聚合成**一条**吸柱候选 WARN，
    真平台落差(裙楼→塔楼 c006 201→22 / c009 38→168)不贴"识别不稳"标签。
    → 公寓 I2 洪峰 37–91 条/栋收敛为 1 条；c022 74 条全消(站板合法)；c006 不再误报。
  - **UA 欠识别**（R3）：`under_recognized` 且确有 ERROR 才标 UNDER 并把 ERROR 降 WARN（c104）；
    稀疏但干净的壳楼(公寓低 LOD)0 ERROR 0 WARN 保持 PASS，不误标。
  - **I10 楼板过小**（R2）：板外柱仍落墙体内（c009 类板盖不住开放柱网区）单列新不变量，正常楼静默。
  首跑判例核查（c006/c009/c104 都只查过"对齐"没查过"柱-板一致性"，是本轮新发现）：
  c006 悬空柱实为忠实图纸的入口门廊柱(处置见 [[qa-structural-invariants]] 三诊) → WARN 待人工取舍；
  c009 统一轮廓(2495㎡)盖不住两翼柱网是真·结构缺陷 → **唯一 FAIL**，需 plan 级从外墙包络重建楼板并区分真天井
  (已出 `data/buildings/c009/plans/floor*.png` 供目视定"开放区是厅缺板还是天井无板")；
  c104 全楼柱 0 + 顶层房间外溢是已知欠识别待重建 backlog → UNDER 不逐条修。
  → 别并入 run_batch（它会重识别覆写 floor JSON）；作为生成后独立回归步骤跑。
- **阶段 3 · 特殊对象化（根除入口污染 + 加阳台/雨棚）**
  给 F0 的台阶/平台/apron 和贴面薄条带加 profile 保护的专项识别分支，从通用墙 union 剥离；
  阳台/雨棚建模为对象。→ c103 入口那次不用再靠克隆。
- **阶段 4 · 天井显式化 + 内立面**
  outline 单孔→登记为"天井开洞"，跨层一致校验、F0 不铺板、内圈墙窗朝孔；
  回字楼（c103 及未来）出专项体检。
- **阶段 5 · 高度与开口的第二信息源（准度上限）**
  侦查 DWG 是否含立面/剖面/门窗表；有则建门窗库回填 + 逐层层高表；
  无则维持 synthetic 但显式标记。**层高全局写死 → 每层可覆盖** 是本阶段必改。
- **阶段 6 · 轴网吸柱 / 曲面与装饰**（锦上添花）

---

## 五、建议的"下一步"

- 收益最高且不碰已调试楼：**阶段 2（结构体检自动化）**——它把你刚定型的"构件对应法"
  变成每次跑批的自动门槛，之后任何楼报错位都能秒级定位到"柱线断在第几层 / 外墙哪一段偏"。
- 若要立刻解决某一类观感：先做 **阶段 3 的入口台阶对象化**（c103 那类"底层鼓"的根）。

---
## 六、给以后的我（速记）

- 建不出"非常规对象"，不是几何能力问题，是**识别没有"对象"概念**——一切线都归了墙/柱/窗。
- 专业做法：**语义对象 + 第二信息源 + 不变量门禁**，三者缺一都是把 bug 当 feature 修。
- 关联记忆：[[arch-rules-over-geometry]]、[[component-library]]、[[misalignment-methodology]]、
  [[floor-misalignment-root-causes]]、[[c103-f0-trim-solved]]、[[su-modeling-reference]]、
  [[chinese-arch-cad-conventions]]。
